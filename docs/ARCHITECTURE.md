# Архитектура: активы, воркеры, функции, ресурсы

Этот документ — общая карта: из чего состоит модель и какой код за какое
понятие отвечает. Он нужен, чтобы новые фичи добавлялись **в логике
парадигмы**, а не превращались в произвольный набор полей и кнопок.

Читать вместе с [`ROADMAP.md`](./ROADMAP.md) (куда двигаемся) и
[`CHANGELOG.md`](./CHANGELOG.md) (как документировать каждое изменение).

## 1. Парадигма в четырёх словах

Модель — это набор **активов**. Актив состоит из трёх вещей, и больше в нём
ничего нет:

- **воркеры** — постановщики, исполнители и проверяющие, люди актива;
- **функции** — то, что эти люди выполняют;
- **ресурсы** — то, что функции потребляют и передают.

Функция может только взять и дать: взять несколько ресурсов и выдать
несколько других, а выданное — передать в другой актив. У каждого входа и
выхода есть **диапазон**: сколько ресурса берётся и сколько выдаётся.
Сначала диапазон закладывается как гипотеза, потом уточняется реальными
выполнениями — по ним показывается среднее арифметическое. У функции есть
**время одного выполнения**.

У функции есть и **расписание**: «непрерывно» (следующее выполнение сразу
за предыдущим) или «раз в N часов/дней/недель/месяцев». Одно другого не
заменяет: работа может занимать час, но делаться раз в месяц.

Из этого выходит весь расчёт: сколько раз функция срабатывает за месяц —
это месяц, делённый на цикл (работа или пауза, что дольше); сколько при
этом уходит и приходит ресурса — это её вилки. Прогноз поэтому не линия, а
лента.

И обратный вопрос, который владельцу нужнее прямого: **поставил цель на
ресурсе — покажи, что для неё надо сделать**. `solve()` разворачивает
цель назад по цепочке функций и отвечает: сколько выполнений какой функции
требуется, сколько это работы и сколько времени займёт.

### Чего в модели больше нет

Прежде связи задавались стрелками «актив → ресурс» с интенсивностью,
периодом, знаком и условиями; поверх них стояли конструктор гипотез, OKR с
ключевыми результатами и поиск рычагов. Всё это убрано целиком, вместе с
`lib/sim.js`, `lib/expr.js`, `lib/hexpr.js` и `HypothesisBuilder.jsx`.

Причина одна: стрелка описывала движение ресурса в обход того, что его на
самом деле производит. У неё не было ни исполнителя, ни длительности, ни
истории выполнения, поэтому работа человека и расчёт модели жили порознь и
неминуемо расходились. Теперь считает то, что делает дело, — функции, — и
работа цепляется туда же.

## 2. Карта понятий: парадигма → код

| Понятие | Где в коде | Как устроено |
|---|---|---|
| Актив | `entities` (`ENTITIES0` в `SystemModel.jsx`) | `{id, name, color, x, y, owners, reviewers}`. Держит воркеров; его ресурсы и функции ссылаются на него полем `e`. |
| Воркеры актива | `entities[].crew` (членство и порядок), `crewOf`, `isCrew` | ОДИН список: «кто вообще работает в этом активе». Роли — постановщик, исполнитель, проверяющий — выставляются У КАЖДОЙ ФУНКЦИИ отдельно (`funcs[].setters/owners/reviewers`), и это единственное место, где их назначают: роль человек исполняет не в активе вообще, а в конкретной работе — одну функцию ставит, другую выполняет, третью проверяет. Списки ролей у самого актива это стирали, поэтому там их больше нет; у прежних моделей они остались, и `crewOf` читает их как членство — люди не пропадают. Люди принадлежат **активу**, поэтому на функцию можно поставить только его воркера (`pruneWorkers` чистит по `crewOf`). Роль у функции — это ДОЛЖНОСТЬ, а не человек: `funcs[].posts = {setters, owners, reviewers}` держит идентификаторы должностей (`positions` в org.json), и работу берёт любой воркер актива с такой должностью. Обратная сторона — `funcs[].except`: список людей, которым эта функция закрыта; в строке воркера («Воркеры» актива) стоят только те функции, к которым он допущен должностью, и нажатие закрывает одну. Кого предлагать, считает `eligible(f, role, {crew, positionOf})` (`lib/funcs.js`), сервер повторяет то же в `funcExecutors` (должности приходят из org.json через маршрут постановки). Старые схемы, где у роли записаны ЛЮДИ (`funcs[].setters/owners/reviewers`), читаются как были: должность не названа — назначать дают записанных прежде. Снятая отметка уносит человека со всех функций актива. В строке списка пять вещей и в этом порядке: должность, имя, статистика по срокам, рейтинг, сколько работ сдано, — и статус, чтобы не открывать карточку ради вопроса «можно ли поручить сейчас». Чего нет — сказано словом («без оценок»), а не нулём, который читается как «оценили на ноль». Здесь же заводится список ДОЛЖНОСТЕЙ (`positions` в `org.json`, маршруты `/api/org/positions`): должность — не роль. Роль отвечает на «что человеку показывать» (вкладки) и выбирается при приглашении в «Людях и ролях»; должность отвечает на «кем он числится» и нужна тому, кто выбирает, кому поручить работу. Один и тот же дизайнер бывает и исполнителем, и проверяющим, поэтому общего списка у них быть не может. Встроенных должностей нет: какие они бывают, знает владелец. Удаление должности оставляет человека без должности (роль не трогает) — молча раздать другую нельзя. |
| Три роли задачи | `tasks[].setter` / `.assignee` / `.reviewer` (`TASK_ROLE`) | Все три обязательны, и срок тоже: `taskGaps()` называет, чего не хватает. Содержимое — НЕ обязательно: что это за работа, уже сказано описанием функции (`funcs[].about`), и оно едет в каждую её задачу; требовать переписывать его в каждое выполнение значило бы спрашивать одно и то же дважды. Постановщику есть что добавить — добавит. Выбирая людей, он видит рядом с каждым именем его статистику (`shortStat`). |
| Оценка 1–10 | `MARK_MIN`/`MARK_MAX` (`lib/workers.js`), `MARK_MAX` в `server/src/lib/ratings.js`, проверки в `workspaceStore.js` и клавиатура в `botTasks.js` | Шкала десятибалльная и ОДНА на всё: форма сдачи (оценка постановки), «Проверка» (оценка выполнения), бот (клавиатура двумя рядами — в один ряд Telegram ужимает кнопки) и проверки сервера. Разойдись клиент и сервер, сервер отбрасывал бы оценки, которые интерфейс дал поставить. Прежние оценки 1–5 читаются как есть: шкала расширилась, а не сместилась. |
| Готовые и удаление задачи | `BOARD` без `done` (`TasksBoard.jsx`), разделы «готовые» и `canKill`/`Delete` (`ReviewBoard.jsx`) | Доска задач отвечает на вопрос «что мне делать», и колонки со сделанным там нет: она отвечала на другой вопрос. Готовые стоят на «Проверке», своим разделом, — принял работу этот человек, ему и видеть результат. УДАЛЯЮТ задачу тоже там, кнопкой внутри раскрытой карточки. Неначатую (`canKill`: не взята, статус ожидает/бэклог/отложено, сдач нет) — тот, кто ставит: терять нечего. Ждущую проверки и готовую — ТОЛЬКО владелец (`canKill(t, {isOwner})`, 2026-09-13): модель его, и решать, нужна ли в ней эта работа, — ему; подтверждение говорит, что уйдут сдачи, оценки и выданные единицы. В работе не удаляет никто — сперва «Отменить» на доске, и задача вернётся в бэклог; у взятой кнопки нет вовсе, а не «есть, но откажет». |
| «Отменить» на доске | `drop()` в `TasksBoard.jsx` | Отменяют РАБОТУ, а не задачу: она в работе, человек её бросает — задача возвращается в бэклог и снимается взятие, иначе висела бы «в работе» у того, кто от неё отказался. У лежащей в бэклоге кнопки нет: её никто не делает, и «отменить» там значило бы «удалить», а удаляют на «Проверке». У сданной и принятой тоже нет: первую проверяют, вторую сделали. Пометка `canceled` осталась в записи и всюду читается (старые записи, «Вернуть»), но новых отменённых задач больше не появляется. |
| Статусы задачи | `STATUSES` в `TasksBoard.jsx` | `ожидает постановки → ожидает → отложено → дедлайн → в работе → проверка → готово`. У бэклога ДВА состояния (`BACKLOG_STATES`): «ожидает» — время ещё не пришло, «отложено» — уже позвали, а работа не началась (человек нажал «Отложить» в уведомлении или промолчал). Колонка при этом одна: перекладывать отложенное на отдельную полку значило бы завести «потом», а его никто не откладывал навсегда. Первый — «это ещё не задача, а намерение её поставить»: пока `isSet()` не сходится, дальше её не двинуть. Дальше задачи НЕ ПЕРЕКЛАДЫВАЮТ: стрелок «‹ ›» на доске нет вовсе. У исполнителя два действия, и оба про работу — «Взять в работу» (`taken`) и «Сдать» (открывает форму сдачи). Остальное — следствие: «Дедлайн» наступает сам, когда срок прошёл, а работа не сдана (`overdue` в `autoStatus`), «Проверка» — от сдачи, «Готово» — от приёма отчёта. Колонка это ответ на вопрос, что с задачей, а не полка: стрелка «переложить» предлагала бы объявить работу сделанной, ничего не сделав. Взятие в работу — отдельная операция на сервере (`POST /api/workspace/tasks/:id/take`, `takeTask`): модель целиком пишет владелец, а брать работу должен тот, кто её делает, иначе нажатие жило бы только в его окне. |
| Срок задачи | `tasks[].end` + `endBy` (`defaultEnd`) | Считается от начала по **верхней** границе одного выполнения с учётом расписания: обещать по нижней значит заранее назначить срыв. `endBy: "auto"` — срок едет за началом; `"hand"` — назначен человеком и не двигается ничем. В расчёт всё равно идут часы, реально потраченные (`submissions[].hours`), а не срок. |
| История воркера | `lib/workers.js` + `PersonStats.jsx` | Что делал, в какие сроки, как приняли. Доля «в срок» и объём **считаются** из задач, средняя оценка — из ОПУБЛИКОВАННЫХ оценок (`model.published`, см. «Оценки без имени»); ничего из этого не хранится числом: хранить значило бы завести вторую правду. `null` вместо нуля там, где данных нет; принятая, но не опубликованная работа подписана «оценка ещё не опубликована», а не «без оценки». Что из этого показывать смотрящему, решает одно место — `visibleStats(model, personId, viewerId)`: про себя человек оценок и рейтинга не видит (`self: true`, `mark: null`), только адресованные ему слова. Чужая СКРЫТАЯ строка постороннему (и владельцу — модель у него целиком, правило сервера повторено здесь) не показывается вовсе — ни отметка, ни слова, ни признак «скрыто» (`strangerHidden`; `historyOf` с `viewer` режет так же); в средние она при этом входит: `statsOf` считает по всем строкам, скрытость прячет отметку от глаз, а не из рейтинга. |
| Оценка за работу | `tasks[].reviews[]` (`mark` 1–5 + `comment` + `hidden`) | Принять можно только с оценкой И словами: оценка без слов не говорит, что исправить, слова без оценки не складываются в историю. Проверяется и в интерфейсе, и на сервере (`reviewTask`). `hidden` — ОДНА скрытость на отметку и слова (v1.2, Q9): скрытую отметку видит только автор, и в средние она входит наравне с публичной (`lib/ratings.js`); скрытые слова — автор и исполнитель (адресат); публичное после публикации видят все. В `ReviewBoard` переключатель один на оба (`HiddenSwitch` из `TasksBoard.jsx`), умолчание — публично: два переключателя позволяли бы спрятать отметку и оставить слова, а по словам отметка угадывается. |
| Оценка за постановку | `tasks[].submissions[].setterRating` = `{mark 1–5 \| null, comment, hidden} \| null` | Обе стороны отвечают за свою половину работы: проверяющий оценивает выполнение, исполнитель при сдаче — постановку. Отметка и слова необязательны; пусто и там и там — `null`, а не нули. Себе не ставится (постановщик = исполнитель): и форма блока не показывает, и сервер не записывает. Проверяющему из сдачи не показывается: оценка про постановщика и доходит до него по правилам публикации. `hidden` — та же одна скрытость на отметку и слова, что у оценки за работу; умолчание — публично (`NO_RATING`). |
| Форма сдачи | `TaskView` в `TasksBoard.jsx` (`newSubmission`, `missingGives`) | Сверху — отчёт словами; ниже часы и числа по ресурсам; потом вещи — по каждому выходу функции кнопка «Загрузить <ресурс>» (обязательные, `requiredGives`, без файла сдачу не пускают; необязательные — кнопка есть, но не держит). Кнопки «Загрузить отчёт» нет: отчёт — слова, файлы — вещи, и файл «вообще» ложился бы мимо ресурса, к которому относится (`submissions[].file` у новых сдач всегда `null`, у старых показывается). Когда отчёт написан и обязательное приложено, НА МЕСТЕ кнопок загрузки появляются оценка постановки, слова, один переключатель «скрыто/публично» и «Сдать»; до этого «Сдать» в форме нет, а словами сказано, чего не хватает — молчаливо неактивная кнопка хуже всего. Кнопка «изменить вещи» возвращает к загрузке, «К оценке и сдаче» — обратно. |
| Оценки без имени | `server/src/lib/ratings.js` (`collectRatings`, `publishStep`, `publishedRatings`, `statsFor`, `commentsFor`, `viewRatingsFor`), реестр `model.published`, `GET /api/workspace/ratings` | Оценка с именем — не оценка, а отношение. Поэтому она публикуется БЕЗ АВТОРА и только когда автора нельзя вычислить: у человека по этому виду оценок не меньше двух от РАЗНЫХ людей (опубликованные и ожидающие вместе); за одну попытку — не больше одной. Попытка — при каждом чтении рейтинга и на тике планировщика. Хранится только реестр опубликованного — идентификаторы `${taskId}~${kind}~${by}`: «опубликовано» — это то, что случилось, из модели этого не вывести; средние же считаются. Реестр ведёт сервер: модель, присланная клиентом, его не затирает (`writeModel`, `PUT /api/workspace`). В опубликованном нет ни автора, ни задачи, ни времени. Свои оценки и свой рейтинг человек не видит (сервер режет отметки в его задачах, `taskViewFor`: отметку решения видит только его автор — отдельная отметка в задаче называла бы автора не хуже имени; скрытые слова — автор и адресат); видит адресованные ему слова, чужие рейтинги и чужие публичные слова. Скрытая отметка входит в средние наравне с публичной. Порог — на человека и вид оценки, за всё время; правила у оценок за выполнение и за постановку одинаковые. |
| Комментарии в задаче | `tasks[].comments[]` = `{id, text, at, by, to, hidden}`, `POST`/`DELETE /api/workspace/tasks/:id/comments[/:cid]`, `canSeeComment` в `TasksBoard.jsx` | Комментарий — это разговор в задаче, и видят его ВСЕ её участники, всегда: ни адресата, ни скрытости форма больше не даёт. Сказать что-то лично о человеке — это ОТЗЫВ, и у него своё место и своя скрытость (оценка постановки у исполнителя при сдаче, решение проверяющего на «Проверке»); поле «кому» превращало разговор в переписку через третьего. Поля `to`/`hidden` в записи остались ради прежних комментариев: их писали как личные, и `canSeeComment` по-прежнему прячет их от посторонних. Убрать комментарий — тоже операция сервера (`dropComment`): владелец — любой, остальные — только свой; иначе убранное возвращалось бы после перезагрузки. Режет сервер (`taskViewFor`) — и в срезе модели, и в ответе на каждое нажатие (`seen` в `routes/workspace.js`): ответ на POST виден в отладчике так же, как ответ на GET; в интерфейсе правило повторено, чтобы и владелец, у которого модель целиком, не читал чужих скрытых слов. |
| Порядок воркеров | `entities[].crew` + `byCrew()` | Порядок задаёт человек стрелками, и в нём же воркеров предлагают в формах выбора у функции: у выбирающего бывают причины, которых в цифрах нет. `byRating()` (оценка → доля «в срок» → объём) остался для тех мест, где вопрос именно «кому поручить»; человек без оценок идёт после оценённых, а не считается худшим. |
| Порядок воркеров при выборе | `entities[].pickByOrder`, `pickByOrderOf`/`pickOrder` (`lib/pickOrder.js`), галочка в `Workers` (`AssetPanel.jsx`), подстановка в `TaskSetup` (`TasksBoard.jsx`) | Порядок списка воркеров ЗНАЧИТ: кого поставили выше, того берут первым — в форме постановки он стоит первым в выборе и подставляется в пустое поле исполнителя/проверяющего при открытии формы. Только в пустое, только у задачи в `wait`, один раз на задачу (`picked`): снятое рукой не подставляется снова. Через `commit`, как ручной выбор, — у позванного постановщика уезжает на сервер. Галочка под списком выключает (`pickByOrder: false`): тогда список по алфавиту и подстановки нет. Поля нет — читается как «включено»: этого владелец и просил, галочка — чтобы отказаться. Сервер и бот ничего не подбирают. |
| Какая схема открывается | `pickScenario()` / `touchScenario()` в `storage.js`, `openedAt` у сценария | Порядок ответов: запомненный в браузере id → самая свежая по `openedAt` → самая свежая по `savedAt` → **рабочая модель владельца с сервера** (её он пишет туда сам, при каждой правке) → встроенная демонстрационная (нужна ровно первому заходу). `openedAt` живёт ТАМ ЖЕ, где сам сценарий (на сервере — в манифесте, `POST /api/scenarios/:id/open`), потому что браузерную память Telegram чистит без предупреждения, а с другого устройства её нет вовсе. «Последняя сохранённая» и «последняя открытая» — разные вещи, и путать их нельзя. Черновик несохранённых правок сильнее автозагрузки, но не глушит её: схема грузится под плашкой, а «Восстановить» перебивает пришедшее (`restored`), потому что черновик новее всего, что лежит на диске. У не-владельца схема одна: та, что приезжает с сервера. |
| Ресурс | `traits` | `{id, e, ks, l, unit}` — то, что есть. Сам он не изменяется: его берут и выдают функции. СКОЛЬКО его есть — не поле, а счёт: единицы в материалах и принятых сдачах минус израсходованное (`stockOf`/`withStock` в `lib/units.js`, зеркало — `server/src/lib/stock.js`). Записанное в старых моделях `have` не читается: число, введённое руками, разошлось бы с вещами, которые можно скачать. Расчёту, доске и карточке ресурса `SystemModel` отдаёт `traitsLive` — ресурсы с посчитанным `have`; правят при этом `traits`, по id. Цели у ресурса НЕТ: см. следующую строку. |
| Материалы | `materials` + `lib/units.js` (`newMaterials`, `newCode`, `normalizeMaterials`), форма `Materials` в `ReportsPanel.jsx` | Единицы ресурсов, заведённые руками: договор пришёл от заказчика, ключи выдали при регистрации. Второй — и последний — источник единиц после сдач; форма на вкладке «Отчёты» перед проектами показывает ВСЕ активы разом (по форме ходят, чтобы видеть, где чего и сколько): актив раскрывается в ресурсы с «есть N», ресурс — в число и кнопки, «Посмотреть» — в единицы (и из сдач тоже) с номерами, скачиванием и историей каждой: из задачи — какая функция руководила, что написали при сдаче, что отдано взамен (`takes` сдачи, с номерами из `took`, если назвали); из материалов — так и сказано, без домыслов. Единица — что-то ОДНО: файл, текст или поле с уникальным кодом (`kind`), и в окне загрузки под выбранным видом ровно одно поле. «Количество N» — N записей ЛЮБОГО вида, по одной на единицу, и у каждой СВОЁ содержимое: N полей файла, N текстов или N кодов в окне (списки в своей прокрутке, кнопка «Загрузить» под рукой; пока заполнены не все — не нажимается, и сказано, сколько из скольких). Один файл на пятерых — одна вещь с пятью номерами, а не пять вещей (`qty` у записи читается ради старых записей и образца, форма его не пишет). Коды рождаются в окне и показываются до загрузки — что человек видел, то и сохраняется. Файл хранится так же, как файл сдачи (`putReportFile`: на сервере или инлайном). Исполнителю сервер отдаёт материалы только видимых ему ресурсов (`viewFor`). |
| Классификация ресурса | `traits[].ks` (`lib/traits.js`) | Их может быть НЕСКОЛЬКО: деньги — и ресурс, и затрата; заявка — и рост, и то, из чего делают работу. Пока классификация была одна, человеку приходилось выбирать, какой правдой пожертвовать. Хранится списком: список из одного — это то же самое поле, а поле из списка не сделать, не потеряв остальные. Прежняя запись с одним `k` читается как список из одного, и старое поле продолжает писаться первым значением списка — его ещё читают снимок для помощника и чужие сценарии, и разъехаться две записи про одно и то же не должны. Ни одной классификации — это ответ («без классификации»), а не поломка: подставлять первую попавшуюся не за что. Удаление вида СНИМАЕТ его с ресурсов, а не переводит их в другой — решать за человека, чем теперь считать вещь, приложение не должно. |
| Чем подтверждается единица | `traits[].kind` + `traitKind`/`kindOfTrait` (`lib/units.js`), карточка ресурса в `AssetPanel.jsx` | Вид единицы — свойство РЕСУРСА, а не загрузки: чем подтверждается договор, решают один раз, когда заводят «договоры», а не каждый раз, когда очередной договор кладут. Спрашивать на каждой загрузке значило бы разрешить сегодня класть его файлом, завтра текстом — и ресурс перестал бы быть одной вещью. Правило одно на ВСЕ двери: «Материалы» на вкладке отчётов и сдача задачи читают его из одного места. Загружают только ФАЙЛ или ТЕКСТ. Уникальный код не загружают — его создаёт программа (`newCode`): набранный руками код не уникален ничем, кроме обещания; вместо него прикладывают ПОДТВЕРЖДЕНИЕ — один файл на всю загрузку и сразу на все её единицы (подтверждают партию, а не каждый ключ отдельно). Не названо у ресурса — файл: так работала сдача с самого начала, и молчание прежних схем значит то, что в них и было. |
| Материалы задачи | `lib/taskUnits.js` (`inputUnits`, `inputCount`, `tookUnits`, `givenUnits`), строка единицы `components/UnitLinks.jsx` (`UnitList`, `unitDownload`), блок «материалы на входе» в `TaskView` и число на карточке доски (`TasksBoard.jsx`), блок «материалы» в `Card` (`ReviewBoard.jsx`) | Что лежит на входе функции и что вышло из сдачи — вещами, а не числом. Вход: по каждому `takes` единицы этого ресурса из `unitsOf` минус `spentIds`; непринятые показаны с пометкой. Сдача на «Проверке»: «взято» — единицы по `sb.took` (не названы → слова «какие именно — не названо» с количеством из `takes`), «выдано» — строки `unitsOf` этой (последней) сдачи, у кода — сам код и ссылка на `sb.proof`. Ссылка одна на всё приложение: файл → `reportSrc`, текст/код → `textHref` (`storage.js`), имя файла как в «Отчётах». `ReviewBoard` получает `materials`, иначе единицы, положенные руками, не находятся по номерам. |
| Сколько вещей в сдаче | `submissions[].units[trait][]` + `proof`, `unitsOf` (`lib/units.js`), `missingGives` (`lib/funcs.js`) | Сколько единиц сдают — столько и прикладывают: сдал десять договоров — десять файлов. Прежде поле было одно на весь ресурс, и число рядом говорило «десять»: одна вещь с десятью номерами — это не десять вещей, и скачать девять из них было неоткуда. Число единиц берётся из самой сдачи (`gives[trait]`, целое — половины вещи не сдают), и «Сдать» не появляется, пока заполнены не все. У кода заполнять нечего — ждут подтверждение, одно на всю сдачу. В `unitsOf` одна такая сдача разворачивается в N строк по одной штуке; ПРЕЖНИЕ сдачи (и сдача из бота, где файл один) остаются одной строкой на всё количество — делить её на десять пустых значило бы завести девять вещей, которых никто не прикладывал. Первая единица носит прежний идентификатор `sb~trait`: на него ссылаются разделы отчётов и записи «взял», и менять его задним числом нельзя. |
| Цель | `goals` (`lib/goals.js`) + `GoalsPanel.jsx` | `{trait, exprs, rate, dueKind/dueIn/dueUnit/dueOn, days, hours, hoursUnit, hoursPer, appliedAt}` — намерение: сколько, чего, к какому сроку, каким темпом и сколько времени на это готовы тратить. Живёт в «Прогнозе», а не на ресурсе: полем «сколько нужно» цель обеднялась до числа, а число не отвечает ни на один вопрос, который человек про цель задаёт. Затрат по другим ресурсам цель НЕ спрашивает: человек называл их наугад, а модель тут же считала настоящие — два ответа на один вопрос, причём один догадка. Осталось посчитанное (`plan.extra`, блок «цена по ресурсам»). |
| Условия цели | `goals[].exprs` + `exprsOf`/`goalState` (`lib/goals.js`), `lib/expr.js`, `ExprField.jsx` | «Сколько» у цели — не число, а УСЛОВИЯ, и их СПИСОК: одним можно сказать только «не меньше» или «не больше», а диапазон — это два сразу («> 10» и «< 50»). Между ними «И»: цель достигнута, когда держатся все. Каждое условие — выражение: знак (`>` `<` `=` `!`), числа, четыре действия со скобками и ссылки `@ресурс` на остаток другого ресурса — ссылка и есть ответ на «от чего это зависит» («> @Заявки*0,5» — не меньше половины заявок). Ссылка хранится идентификатором, показывается именем: переименование её не рвёт. Плану достаётся НИЖНЯЯ граница — самое большое из «=» и «>» (`target`): по меньшему он встал бы раньше, чем цель достигнута; верхняя (`cap`, самое маленькое из «<») план не двигает, а проверяется. Условия, при которых нужного числа не существует («> 10» и «< 5»), названы спорящими и не планируются вовсе: считать по одному из них значило бы считать другую цель. Старая запись — одна строка `expr` или число `qty` — читается как список из одного. |
| Применение цели | `appliedAt` + `copyGoal()` в `lib/goals.js`, `onDropGoal` в `SystemModel` | Цель применяется ОДИН раз. Повтор — не второе нажатие на ту же цель, а её копия рядом в списке: две работы дают два разных результата в разные сроки, и записаны они должны быть двумя строками, иначе непонятно, чей результат чей. Удаление цели снимает и её незаконченную работу с доски, и весь прогноз считается заново: оставшиеся задачи означали бы работу, которую никто уже не просил. Сделанное и сданное при этом не трогается — оно случилось. |
| Одновременных выполнений | `funcs[].par` и `funcs[].parAll` + `parWorkerOf()`/`parAssetOf()`/`parOf()` (`lib/funcs.js`), `cycles`/`runsHours`/`workHours` (`lib/plan.js`) | Пределов ДВА, и это разные вопросы: `par` — сколько таких дел ведёт разом ОДИН воркер, `parAll` — сколько их идёт В АКТИВЕ вообще (станок один, кабинет один, лицензий три). В счёт идёт меньший (`parOf`): людей мы не считаем — умножать «на воркера» на их число нельзя (инвариант 6), — а потолок актива обязан работать. Пустой `parAll` значит «предела нет», а не «одно»: молчание — не запрет, и прежние схемы с восемью одновременными делами остаются какими были. Потолок актива можно не называть числом, а привязать к людям — `funcs[].parCrew`: «разом идёт столько, сколько в активе воркеров». Тогда число не правят при каждом новом человеке, и второй записи того же самого не заводится; поле числа при этом убирается — спрашивать уже сказанное незачем. Считается это ОДИН раз, на входе в расчёт (`withCrewPar`): `parOf` спрашивают из `cycles`, `runsHours` и `workHours`, а они знают только саму функцию, и тащить через них состав актива было бы нечем оправдать. Воркеров нет — считаем как одного: ноль читался бы как «предела нет», а «работать некому» — про другое. Сервер (снимок отчёта) повторяет то же правило. Настройка про долгие работы: юрист ведёт восемь дел месяцами, и выстраивать их друг за другом значило бы обещать восемь месяцев там, где выйдет один. Из одного правила — «одно выполнение занимает 1/par времени воркера» — следуют все три числа сразу: за месяц успевается `par` выполнений (`cycles`), `n` выполнений идут волнами по `par` (`runsHours`, расписание ставит задачи волнами, а не очередью), и времени человека они стоят `n/par` длительностей (`workHours`). Иначе настройка меняла бы сроки и тут же обвиняла в восьмикратной перегрузке того, для кого её и завели. Это НЕ множитель от числа людей — см. инвариант 6. Измеренные часы из сдач на `par` не делятся: факт поправлять допущением нельзя. |
| Отменённая задача | `tasks[].canceled` + `isCanceled()` (`TasksBoard.jsx`) | Работу ОТМЕНЯЮТ, а не стирают: удаление уносило вместе с задачей её сдачи, оценки и то, что по ней успели сделать, — а это было. Отменённая остаётся в списке с пометкой и перечёркнутым названием, и её можно вернуть. Отменяют только ту работу, которой ещё нет: у задач НА ПРОВЕРКЕ и ГОТОВЫХ кнопки нет — сданную проверяют, принятую уже сделали. Работой не считается нигде: не идёт в факт расчёта (`runsOfFunc`, `actualOf`), не порождает единиц ресурса (`unitsOf`), не входит в историю человека (`historyOf`), не напоминает о себе (сервер, `scheduler`), не берётся и не сдаётся. Отдельным полем, а НЕ статусом: статус считается сам (срок прошёл — «Дедлайн», сдали — «Проверка»), и отмена, положенная в него, затиралась бы первым же пересчётом; к тому же отменяют и ждущую, и взятую, и сданную — это ответ на вопрос «делаем ли мы это вообще», а не место в очереди. |
| Готовность функции | `funcs[].accepted` + `funcGaps()` / `funcState()` (`lib/funcs.js`) | Проверки говорят, что функция СОБРАНА; «принята» — слово человека: заполнено может быть всё, а автор ещё думает. Отсюда три состояния при двух цветах: красное «не заполнена» (и названо, чего не хватает), красное «не принята» (собрана, кнопку не нажимали), зелёное «готова». Цветом сказано только «можно этим пользоваться или нет», различает состояния подпись — цвет один не несёт смысла. «Принять» нажимается ВСЕГДА: пробелы, которые находит проверка, — подсказка рядом с подписью, а не условие; принятая функция зелёная, даже если проверка ещё что-то находит, — слово человека старше подсказки. Любая правка снимает «принято»: зелёная метка на изменённой функции врала бы про то, что человек её видел. `funcGaps` — один список на два места: он красит подпись и называет, чего нет, он же перечислен в описании функции; два ответа на один вопрос разошлись бы в первый же день. |
| Технологический процесс | `procs[]` в документе: `{id, name, text, status, steps, hypo, missing}`; `lib/process.js` (`newProc`, `normalizeProc`, `tokenize`/`parseLine`/`parseProcess`, `resolveProc`, `stateOf`, `procIssues`, `procFuncs`, `syncProcFuncs`, `dropHypo`, `procUsesAsset`, `hintAt`/`suggestNames`/`replaceName`); раздел `ProcessPanel.jsx` первым на «Управлении» (`ProcText` — поле и всплывающее окно подсказки, `Chip`; спойлер `procsOpen` в `SystemModel`) | Живёт на «Управлении», первым и под спойлером: до нажатия процессов не видно, чтобы форма не заслоняла карточку актива; своей вкладки под схемой у процессов нет (2026-09-13). Выбранный на схеме актив подсвечивает процессы, где он занят (`procUsesAsset`), и число их стоит в заголовке спойлера. Описание — ТЕКСТ, одно поле (владелец, 2026-09-13: «как была полем ввода, так и должна остаться»). Строка — шаг, в порядке, названном владельцем: `Актив, Должность, берёт: Откуда, Что [сколько], …, отдаёт: Куда, Что [сколько], …`; метки `берёт:`/`отдаёт:` (`TAKE`/`GIVE` — «выдаёт», «даёт», без двоеточия тоже) делят входы и выходы, стрелок нет, запятая перед цифрой — десятичная (`tokenize`). Подсказка — только всплывающее окно под полем, пока поле в фокусе (`hintAt` по положению курсора → чего ждут: актив, должность, метка, актив пары, ресурс; `suggestNames` — подходящие имена, набранное сужает, заведённое из процесса первым, от нового к старому); Tab/Enter подставляет первое, мышь — любое, но выбирать не обязательно: имя вводится и руками. Под полем — разбор строки чипами: найденное — кнопка с именем, нажатие открывает карточку сущности под формой (актив — карточку, ресурс — «Ресурсы», должность — «Воркеров» актива; `focus.kind`); неизвестное — пунктир с «Принять» (актив — гипотезой рядом с существующими, ресурс — гипотезой в названном активе пары, должность — на сервере через `onAddPosition`) и «Отклонить» (`missing.rejected`, «Вернуть»); удалённое на схеме — красный чип с выбором замены из ресурсов того же актива, и выбор правит слово в тексте (`replaceName`), а не id мимо него. Ресурс ищется только в названном активе пары (`findTrait`). Название процесса — по нажатию справа от слова «процесс» (`name`; `procLabel` — имя или первая строка). ТЕКСТ — единственный источник: по расфокусу поля `resolveProc` пересобирает `steps` (память id по именам: переименованное на схеме остаётся своим, удалённое видно удалённым), а `syncProcFuncs` — обычные функции с пометкой `proc` (по одной на строку, `${proc}_${line}`, в активе строки, `lo = hi`, `accepted: true`, должность — в `posts.owners`); они живут в `funcs` наравне с остальными — второго расчёта «для процессов» нет. Три статуса: `off` (функций нет, гипотезы убраны), `hypo` (считается с галочкой), `on` (считается всегда). Принять нельзя, пока есть ошибки строк или неразрешённые имена (`procIssues` — словами, по строкам; в строке достаточно хотя бы одной пары — только «берёт:» или только «отдаёт:» допустимо). Ввод выборами (`Combo`/`PortRow`) с этого же дня убран по слову владельца; записи в старом виде читаются `normalizeProc` (`text` — по `formatStep` из шагов, если его нет). |
| Включить гипотезы | `hypoOn` в `SystemModel.jsx` (состояние интерфейса, в документ не идёт); `activeFuncs`/`liveModel` в `lib/funcs.js` | Функции процессов `hypo` считаются только при `model.hypoOn === true`; `on` — всегда; функция процесса, о котором модель не знает, — как принятая. Все расчёты входят через `liveModel` (прежде `withCrewPar`): одна дверь, чтобы прогноз, нагрузка, план цели и цепочка считали одну и ту же схему. Галочка стоит над «Прогнозом» и «Деятельностью» и только когда есть процессы в `hypo`. |
| Принят ресурс / фактор | `traits[].accepted`, `factors[].accepted`; `Traits`/`Factors` в `AssetPanel.jsx` | Как у функции: слово человека, кнопка «Принять», красная точка и подпись «не принят», пока не нажали; любая правка (у ресурса — имя, единица, вид, классификации; у фактора — имя, вероятность) снимает пометку, расфокус без правки — нет. Отсутствие поля в прежней записи = не принят. Проверки строения (`checkTrait`, `checkAsset`) пометку не читают — она про одобрение, не про строение. |
| Что цель означает | `planGoal()` в `lib/goals.js` | Из темпа — сколько ресурса в месяц; из модели — сколько выполнений и человеко-часов; дальше три прямых ответа: влезает ли работа в бюджет времени, успевает ли первый результат к сроку, держится ли темп (цикл длиннее периода — «раз в неделю» не выйдет). Плюс цена по ресурсам: названная человеком рядом с посчитанной. |
| Что цель сделает с ресурсами | `effect()` в `lib/plan.js` | Чистое изменение по каждому ресурсу за один круг работы: чего прибавится, чего убавится. Чистое, а не приход и расход по отдельности — ресурс, который функция и производит, и потребляет, двумя строками только пугал бы числами, гасящими друг друга. |
| Во что цель превратится на доске | `scheduleOf()` в `lib/plan.js` + `steps[].startHours` | Список будущих задач с датами: функция начинается не раньше, чем созреют её входы, а её выполнения идут одно за другим. Показывается ДО применения: когда именно придётся работать — половина ответа на вопрос «потяну ли». |
| Три состояния цели | `shown`/`fresh` в `Goal` + `goals[].appliedAt` | **Задана** → кнопка «Спрогнозировать»; **спрогнозирована** → показан весь расчёт и кнопка «Применить цель»; **применена** → задачи заведены. Правка любого поля намерения сбрасывает прогноз обратно: применять числа, которых человек не видел, нельзя. Отпечаток (`stamp`) сравнивает поля намерения, а не всю запись — отметка о применении к расчёту отношения не имеет. |
| Последовательность действий | `actionsOf()` в `lib/goals.js` | Шаги плана по порядку начала: функция не начинается раньше, чем созреют её входы, поэтому это очередь, а не список вперемешку. Одна и та же функция считает очередь и в форме цели, и в общем «Прогнозе» — разъехаться они не должны. |
| «Что если сделаем вот это» | `ifDone()` в `lib/goals.js` | Галочки у шагов: человек отмечает, во что верит, и видит, докуда дойдёт целевой ресурс. Шаг, не дающий целевой ресурс, назван промежуточным — «прибавится на 0» выдавало бы нужную работу за пустую. |
| Цель как показатель | `Gauge` в `GoalsPanel.jsx` | Сколько есть против того, сколько нужно. Полоса — только у разовой цели: у цели с темпом уровня нет, и сравнивать темп с остатком на складе бессмысленно — полоса врала бы. |
| Применение цели | `goals[].appliedAt` + «Применить цель» | Граница между прикидкой и решением. Неприменённая цель считается, но ни на доску задач, ни на графики не влияет: её крутят и смотрят, что выйдет. Применение заводит задачи (`goalId` на каждой) и включает планку цели в график. Без этой границы каждая правка числа молча меняла бы доску, и попробовать «а что если» было бы негде. |
| Время на достижение | `goals[].hours` + `hoursUnit` + `hoursPer` + `budgetHours()` | Число, ЕГО ЕДИНИЦА и период: «2 ч в день», «2 дн в неделю». Без единицы «2 в день» не читается вовсе, а поле молча считало часы. Мера та же, что у сроков функций (`DUR_UNITS`): день это 24 часа — двух разных «дней» в одной модели быть не должно. Прежние цели единицы не знали, и «ч» по умолчанию оставляет их числа теми же. Бюджет идёт в прогноз: `planGoal` приводит его к месяцу и сравнивает с посчитанной работой (`fits` / `fitsSure`), и это сравнение стоит на экране рядом с самой работой. |
| График работ у цели | `goals[].hours/hoursUnit/hoursPer/days` + `budgetHours()`; раздел «в график работ» в `GoalsPanel.jsx` | Раздел назван по слову владельца (2026-09-13) «в график работ»: флажок «учитывать график», под ним графа «рабочее время» (сколько, единица, за период) и графа «рабочие дни». Дни — не украшение: «час в день» по будням — пять часов в неделю, а не семь; пусто — все семь. Трогают они РОВНО бюджет времени, поэтому стоят под ним. Флажок снят — время и дни НЕ пропадают, а гаснут и не нажимаются (владелец: «установка времени не должна пропадать»); в поле при этом стоит запомненное число (`lastHours`), а не ноль из записи. Второго поля «учитывать ли» в записи цели НЕТ: бюджет это `hours`, и ноль в нём и есть «не учитывается» — признак рядом с числом рано или поздно разошёлся бы с ним. Срок цели («к какому сроку») — одной строкой: кнопки «через»/«к дате» и рядом либо «сколько» с единицей (узкие, `Row w`), либо дата; без переноса, чтобы поля не растягивались на всю ширину телефона. |
| Чем функция выполняется | `funcs[].kind` = `task` / `factor` (`FUNC_KINDS`) | «Задача» — работу делают люди: есть постановщик, исполнитель и проверяющий, и из неё берутся задачи на доске. «Фактор» — то, что происходит без человека: сезон, износ, курс. Ресурсы фактор меняет так же, но спрашивать с него некого — ролей у него нет, задач по нему не заводится (`scheduleOf`), его часы не идут ни в человеко-часы (`solve`), ни в нагрузку (`load`). Календарный срок при этом остаётся: ждать фактор всё равно приходится. |
| Фактор | `factors` (`newFactor`) + вкладка «Факторы» | `{id, e, name}` — принадлежит активу, как ресурс. Отдельная запись, а не галочка на функции: фактор один, а функций от него может быть несколько («сезон» поднимает спрос и роняет внимание), и галочкой он существовал бы столько раз, сколько на него ссылаются. |
| Функция | `funcs` (`lib/funcs.js`) | `{id, e, name, takes, gives, dur, durUnit, owners, reviewers}`. Живёт внутри актива. Две формы, а не три. |
| «И» и «ИЛИ» на входе | `takes[].group` + `groupsOf()`, `pickAlt()`, `takesOf()` | Входы одной группы — варианты одного требования («или»), разные группы соединены «и». Количество у варианта и есть курс обмена: «2 А» рядом с «3 В» значит, что три В заменяют два А; отдельного поля для курса нет — оно спрашивало бы уже сказанное. Вариант выбирается по ПОКРЫТИЮ (`остаток / сколько нужно`), а не по остатку в штуках: сто при нужде в пятьдесят — это два выполнения, десять при нужде в одну — десять. Пусто везде — первый по списку: порядок вариантов задал человек. Выбор один и тот же в прогнозе и в плане под цель — иначе они говорили бы о разных мирах. У ВЫХОДА группы нет: «выдаст А или Б» без правила, что именно, — монетка, а не модель. |
| Вход и выход функции | `funcs[].takes[]`, `funcs[].gives[]` | `{id, trait, lo, hi}`, у выхода ещё `to`. В записи всегда две границы `lo..hi`; равные — это и есть точное количество («ровно 2»). В форме количество одним полем, а вилка — по галочке «диапазон» (`PortQty` в `AssetPanel.jsx`): чаще всего берут и выдают ровно столько-то, и два поля заставляли писать одно число дважды. Галочка в модель не пишется: «от 3 до 3» и «ровно 3» — одно и то же, и хранить, каким способом это набрали, незачем; открывается она по тому, различаются ли границы. |
| Расходует или обрабатывает | `takes[].spend` (`portSpends`), `runSide` (списание и `seen`), `solve` (`keep`/`held`) | Два разных способа взять ресурс. **Расходует** — взятое исчезает у всех: ни эта функция потом, ни соседняя того же месяца его не получат. **Не расходует** — единица остаётся на месте и достаётся другим функциям, но ЭТА по ней уже отработала: прочитанную заявку не читают второй раз. Правило «второй раз не берём» принадлежит одной функции, а не всем сразу — прежде расходовалось всё и всегда, и прочитанное пропадало у соседей. Не сказано — расходует: молчание прежних моделей их смысла не меняет. Сколько ресурса на одно выполнение — вилка функции; сколько выполнений — считается по целям. |
| Единица ресурса и её номер | `lib/units.js` (`unitsOf`, `unitLabel`, `doneBy`, `heldBy`, `spentIds`) | Количество говорит, сколько всего, и молчит о том, ЧТО именно. Единица рождается СДАЧЕЙ задачи или кладётся в «Материалы» (см. выше): у неё есть автор, дата, содержимое и порядковый номер внутри своего ресурса — общий на оба источника, по времени. Номер сам не вводится: он ставится тому, что есть. По номерам считается «эта функция уже обработала», на них ссылаются разделы отчёта, по ним же (`took` при сдаче) помечаются израсходованные. Тем же правилом номера считает сервер (`reportView.js`): снимок обязан звать вещь так же, как зовут внутри. |
| Передача в другой актив | ничем: следствие того, чей ресурс на выходе | Ресурс принадлежит активу (`traits[].e`), и функция выдаёт его туда, где он есть. Выдать чужой ресурс и значит передать туда. Отдельного поля-получателя нет: оно спрашивало бы то, что уже сказано выбором ресурса, и могло с ним разойтись — выбран ресурс одного актива, получателем назван другой, и что тогда правда, не знал никто. |
| Откуда берутся входы и выходы | одно поле на всю схему (`Ports` в `AssetPanel.jsx`) | И вход, и выход выбираются из ОДНОГО списка, где лежат все ресурсы схемы — свои и чужие, сгруппированные по активам. Двух списков («свои кнопками» и «чужие отдельным полем») нет: функции всё равно, чей ресурс она берёт, и спрашивать об этом значило задавать вопрос не по делу. |
| Время одного выполнения | `funcs[].dur`..`durHi` + `durUnit` (`hoursRange`, `hoursOf`) | Вилка, как и количества: «от 2 до 4 часов». Одно число было бы обещанием, которого никто не давал. Стороны разводятся как у ресурсов: щедрая оценка берёт быструю работу, осторожная — долгую. Равные границы («одинаковое» в форме) — точный срок. Обе больше нуля: «от 0 до 4» говорит «может быть, мгновенно». |
| Следующая попытка через | `funcs[].every`..`everyHi` + `everyUnit` (`everyRange`, `everyOf`) | Вилка, как и длительность. Слово «попытка», а не «повторение»: попытка может и не удаться — у фактора есть вероятность. Ноль — «сразу», упирается только в саму работу. Потолок за месяц — `cycles()`; щедрой оценке достаётся частая попытка, осторожной — редкая. |
| Факторы функции (интерфейс пока убран: `FACTORS_ON` в `lib/flags.js`, 2026-09-13 — модель и расчёт остались) | `funcs[].factors[]` (`factorsOf`), `chanceOf`, `factorHit` в `plan.js` | Просто СПИСОК, который функция перебирает за одну попытку: пробует первый; вышло — попытка удалась и до второго дело не доходит; не вышло — пробует второй, и так до конца. Хватило любого — удалась; не удался ни один — не случилось ничего. Не «должны случиться все»: перемножение означало бы «нужны все сразу», и тогда запасной вариант вредил бы — чем больше путей, тем реже срабатывание. Вероятность считается через обратное: два фактора по 60% дают 84%, а не 120% и не 36%. |
| Вероятность фактора | `factors[].chance` (`factorChance`) | Проценты, и живёт у САМОГО ФАКТОРА, а не у функции: сезон удачлив одинаково, сколько бы функций от него ни зависело, и спрашивать её у каждой значило бы задавать один вопрос по нескольку раз. У задачи её нет вовсе. |
| Жребий | `draw()` в `lib/plan.js` + `seed` | Каждая попытка фактора — отдельный жребий, а не доля в среднем: один и тот же набор чисел может развиться по-разному, и посмотреть на эти варианты — то, ради чего вероятность и заводят. Жребий посеянный: от одного семени выходит один и тот же ряд, иначе прогноз менялся бы при каждой перерисовке и сравнить два варианта было бы не с чем. Кнопка «↻ другой вариант» меняет семя. Недобранные попытки копятся (`credit`): фактор с попыткой раз в полгода делает одну попытку раз в шесть месяцев, а не шестую часть попытки каждый месяц. |
| Задача ждёт ресурсов | `shortage(f, traits, done)` в `lib/funcs.js` | Количество ресурсов меняется само по себе, поэтому задачу можно описать заранее, а поставить — только когда ресурсов хватает. У входа, который не расходуется, считается НЕОБРАБОТАННОЕ этой функцией (`heldBy` по её принятым задачам): ресурс лежит на месте, но второй раз ей не даётся. У расходуемого входа обработанное ни при чём — там всё видно по самому остатку. |
| Один человек по обе стороны | `selfSet` / `selfReview` / `autoStatus` / `autoFlow` в `TasksBoard.jsx` (+ `submitTask` на сервере) | Постановка и проверка — это передача работы другому человеку. Совпал постановщик с исполнителем — задача ставится сама и сразу идёт в БЭКЛОГ (не в «ожидает постановки» и не в «дедлайн»: её не у кого ставить). Совпал исполнитель с проверяющим — сдача принимается сама, задача уходит в готовые (без оценки: ставить её некому). Все трое один — задача просто лежит в бэклоге, а после сдачи оказывается в готовых. Автоматика избавляет от НАЖАТИЯ, а не от работы: задача всё равно должна быть описана (`isSet`) и ресурсов на неё всё равно должно хватать (`shortage`). Ниже `floorStatus()` задача не опускается — возвращать её в «ожидает постановки» бессмысленно, она тут же поставится снова. Правило живёт в `SystemModel` одним эффектом на всё приложение, а не на доске: оно не должно зависеть от того, какая вкладка открыта, — и повторено на сервере, иначе бот и доска говорили бы разное про одну задачу. |
| Где ставят задачу | `TaskSetup` во вкладке «Проверка», прямо под своей строкой; `TaskView` на доске задач | Постановка и приём — работа одного и того же человека: НЕ того, кто делает. Поэтому обе на одной вкладке, а на доске исполнителя формы постановки нет. Форма раскрывается ПОД той задачей, на которую нажали: общим блоком внизу страницы она отвечала бы на вопрос «какую задачу мы ставим» тем, что человек должен вспомнить сам. Обязательны три роли и срок; СОДЕРЖИМОЕ — нет: что это за работа, уже сказано описанием функции (`funcs[].about`), и оно едет в каждую её задачу. Форма — только постановка: постановщик в ней словом, а не выбором (он назначен на схеме, в ролях функции, и задача из цели рождается с ним — `onTasks` в `SystemModel` берёт `funcs[].setters[0]`; старой задаче без постановщика форма подставляет его при открытии); «предупредить за» — не её поле, а настройка того, кому напоминают (`warnMin`); «Удалить» — не её кнопка, а владельца на карточке доски, с подтверждением словами; комментарии — только чтение (`Comments readOnly`), слова к постановке пишет исполнитель при сдаче. Людей в форме назначает тот, кто ставит: постановщик этой задачи или владелец (`canAssign` в `ReviewBoard`). У позванного постановщика постановка — своя операция сервера, как «взять» у исполнителя: `POST /api/workspace/tasks/:id/setup` (`setupTask`; только постановщик задачи или владелец; поля — название, содержимое, начало, срок, исполнитель и проверяющий из воркеров актива функции; только пока задача ждёт постановки), `status: "backlog"` за «Поставить» — с той же проверкой, что у кнопки (`whyNotSet`, повторён в `server/src/lib/taskRules.js`), отказ словами в `why`; форма (`onSetup`) шлёт каждую правку и меняет статус только по ответу сервера. Имена воркеров позванному кладёт в срез сервер (`people` в `GET /api/workspace`, `peopleOf`): список организации целиком — владельцу. |
| Отчёты · карта проектов | `reports` в документе, `lib/reports.js`, `ReportsPanel` | Проект и раздел — ОДНА и та же запись, разница только в том, есть ли родитель: карта складывается фрактально, вложенность без предела, и на каждом уровне тот же блок. Внутри блока сказано, что в него попадает — пара «функция + ресурс»: результаты какой функции и по какому ресурсу. Строки собираются из уже сделанных сдач (дата, кто, часы, количество, файл), а не переписываются руками: переписанное разошлось бы с тем, что было. Принятое и непринятое различены. Потерявший родителя блок всплывает наверх, а не пропадает; удаление уносит всё вложенное. |
| Что показывает раздел отчёта | `reports[].trait` + `.file` + `.upto` (`lib/chain.js`, `lib/reportDoc.js`) | Раздел не «фильтр по сделанному», а ПРОСЛЕЖИВАНИЕ. Человек называет две вещи: ресурс, с которого начинается работа (и прикладывает его самого файлом — вот это техническое задание), и звено, до которого вести. Всё остальное считается: `chainOf` идёт ВПЕРЁД по слоям (ресурс → функции, которые его берут → их выходы), `estimateRange` даёт предварительную оценку вилкой, `factorsIn` называет факторы, трогающие ресурсы цепочки, `actualOf` — факт по принятым сдачам. Звеном бывает и ресурс (закрывает цепочку на себе), и функция (входит последней); не дошли до звена — это разрыв, и он назван. Счёт вперёд, а не `solve()`: цель разворачивается назад от желаемого, а здесь вопрос обратный — «вот это лежит на столе, во что оно превратится». |
| Раздел про определённые единицы | `reports[].units[]` + `familyOf()` (`lib/reports.js`), `descendantsOf` / `parentsOf` (`lib/units.js`) | Раздел отвечает про ВЕЩИ, а не про поток по функциям. Единицу здесь ВЫБИРАЮТ из уже сделанных — по номерам, сколько угодно, а не одну: вопрос «что будет с этими тремя договорами» задают чаще, чем про один. Загрузки нового ресурса в отчёте нет вовсе: она заводила вещь мимо работы — без задачи, без автора и без номера, и сослаться на такую было нечем. Новые вещи рождаются только при сдаче выполненной задачи. Сколько выбрано — столько и считает предварительная оценка, и число это не правится руками. Показано только то, что выросло ИЗ НИХ: сами единицы, их потомки и задачи, которые их сделали. Сама единица из отчёта не выпадает, даже если сделавшая её функция лежит ДО цепочки — выбросить из отчёта о вещи работу, в которой вещь родилась, было бы нелепо. |
| Порядок счёта в цепочке и застрявший шаг | `estimate()` в `lib/chain.js`, то же в `reportView.js` | Цепочка кладёт функцию в список по ПЕРВОМУ достигнутому входу, а входов у неё бывает несколько из разной глубины («передать заказ» берёт и договор, и контакты лида). Поэтому оценка строит очередь заново: следующей считается та функция, все входы которой из цепочки уже кто-то выдал. Шаг, которому всё равно не хватило входа, из списка НЕ исчезает: у него ноль выполнений, нет полосы на шкале и названо, чего не хватило и какой шаг это израсходовал (`short`). Прежде такой шаг молча пропускался вместе со всем, что шло за ним, — и цепочка из четырёх звеньев показывалась одной задачей без единого слова о причине. |
| Раздел без выбранных единиц | `hypothetical` в `reportOf()` / `snapshotOf()` | У раздела ровно ДВА вопроса, и оба про одну вещь: выбрана единица — «что происходило вот с этим договором» (её задачи, её вещи, её часы); не выбрано ничего — «что произойдёт, когда договор появится» (чистый прогноз на `qty` новых). Третьего вопроса — «покажи весь поток по функциям» — нет: отслеживая ОДИН контакт лида, человек видел четыре одинаковых «передать заказ разработчикам» (работу над четырьмя чужими контактами) и справедливо не понимал, при чём тут его. Поэтому поле выбора единицы стоит РЯДОМ с самим ресурсом — вопрос один и тот же, «какой договор», — а не отдельным рядом кнопок ниже, где его не находили. |
| Что из чего сделано | `submissions[].took` = {ресурс: [номера единиц]} | Записывает исполнитель при сдаче, отмечая взятые единицы. Количества говорят, что израсходована одна заявка, и молчат о том, чья, — а спрашивают потом именно об этом. Задним числом связь не восстановить ничем, кроме догадки по датам, и такая догадка хуже отсутствия: она выглядит знанием. Поэтому где не отметили — там `took` пуст, и отчёт по единице прямо говорит, что связь не записана (`traced`). |
| Пять разделов отчёта, у каждого своя тема | `ReportsPanel` (`Part`, `FuncRows`, `Schedule`, `TaskList`, `FactorRows`), `reportHtml()`, `ShareView` | Разделы одни и те же в каждом блоке, включая корневой проект, и слово в слово те же в скачанном файле и в снимке для заказчика: 1) **Ресурсы — что изменится** (сейчас / прогноз-вилка / факт по каждому ресурсу, «нужно со стороны»; ни часов, ни сроков), 2) **Функции — что будет сделано** (цепочка по порядку: берёт, даёт, выполнений ожидается, зависит от факторов; «не выполнится: не хватает…»), 3) **Сроки и трудозатраты** (вся цепочка: займёт, человеко-часов, по факту; таймлайн; по функциям: начнётся, займёт, работы), 4) **Задачи — что уже сделано** (принято N из M; «как эти вещи появились»; задачи по функциям — кто, срок, состояние, по плану/по факту, взяла → вышло), 5) **Факторы — что влияет** (только если есть). Под заголовком каждого — строка, что в нём. Одно и то же в двух разделах не стоит: прежде «прогноз ресурсов» открывался часами работы, а каждая функция была отдельным «разделом отчёта» с оценкой, ресурсами и задачами — и владелец справедливо спросил, зачем ему в ресурсах, сколько функция займёт времени. Ссылка (якорь) есть у функции в разделе 2 (`stepAnchor`, `shag-<блок>-<функция>`), а не у части страницы: ссылаются на «передачу заказа разработчикам», а не на «первый блок сверху». Якорь считается из блока и функции, а не из порядкового номера: номер меняется от любой правки модели, и вчерашняя ссылка вела бы назавтра в другое место. Кнопки «+ раздел внутри» нет: разделы размечаются сами. Считает всё `reportOf()` один раз: экран, скачанный файл и снимок для заказчика обязаны быть одним ответом, а не тремя. |
| Таймлайн шагов | `Timeline` в `ReportsPanel`, `timelineHtml()` в `reportDoc.js` | Отчёт нужен СНАЧАЛА для планирования конкретных работ: «что за чем идёт и сколько тянется». Списком это не читается — «начнётся через 1 ч» и «займёт 1 мес» человек складывает в уме. Шкала — часы от «сейчас», потому что план считается вперёд от этого мгновения; заведённые задачи ложатся на ту же шкалу по своим срокам, и видно, попадает ли работа в план. Задача без дат полосы не получает: нарисовать её «где-нибудь» значило бы придумать срок, которого никто не ставил. |
| График изменений | `ChangeChart` в `ReportsPanel`, `barRow()` в `reportDoc.js` | У каждого ресурса СВОЯ шкала: доход в сотнях тысяч и договоры в штуках на общей шкале превращают договоры в невидимую чёрточку, а складывать их всё равно незачем. Сравнивают план с фактом — это сравнение внутри строки честное. План рисуется вилкой: плотная часть — «не меньше», полупрозрачная — «а может и до». Числа стоят ТЕКСТОМ рядом с названием, а не подписями внутри картинки: подписи рисовались за краем svg и при больших значениях вылезали из формы. Ноль ровно нулём полосы не рисуется — обрубок в два пикселя читался бы как «чуть-чуть». |
| Числа функции правятся в модели | `funcs[].dur/durHi`, порты | В отчёте их не правят. Кнопка «прикинуть иначе» (`reports[].tweaks`) заводила у одной функции столько разных «сколько это займёт», сколько заведено разделов, — и ответ на вопрос «а если дольше?» зависел от того, в каком отчёте спросили. Спорить с числом надо там, где оно задано. |
| Скачать отчёт | `reportHtml()` + `deliverReport()` в `lib/reportDoc.js` | Один HTML-файл: открывается везде, печатается в PDF одним нажатием и остаётся читаемым текстом. Ни скриптов, ни картинок внутри — файл ничего не тянет из сети и не рассыпается через год. Внутри Telegram обычное скачивание (`blob:` + `<a download>`) не срабатывает вовсе — окно молча проглатывает переход, и кнопка выглядит сломанной. Поэтому там файл сперва кладётся в своё же хранилище отчётов, а потом открывается ссылкой (`tg.openLink`); в браузере остаётся прямое сохранение. Ошибку сохранения кнопка называет вслух: молчащая кнопка — та же поломка. |
| Ссылка на блок наружу | `POST /api/shares` → `shareStore.js` + `?share=<токен>` (`ShareView.jsx`) | Отчёт нужен не себе: заказчику показывают сделанное ссылкой, без аккаунта и без приглашения в модель. Ссылка ведёт на СНИМОК блока, а не в приложение: пускать по ней в живую модель значило бы ссылкой на один раздел открывать всё. Снимок собирается на СЕРВЕРЕ из общей модели (`reportView.js`) — принятый от браузера, он показывал бы наружу то, что прислал браузер. В нём развёрнутые имена и только принятые работы. Чтение по токену — без подписи (как у файлов отчётов); заводит, обновляет и убирает ссылки только владелец, и удаление ссылки — это отзыв доступа, действующий сразу. На блок — одна ссылка: вторая означала бы, что отозвать доступ можно только угадав, какую кому отдали. |
| Откуда берутся задачи | применение цели (`GoalsPanel.apply`) | Только из применённых целей: работа, не следующая ни из какой цели, — это работа, о которой никто не спросил, зачем она. Кнопки «+ выполнение» на доске больше нет. Задача из цели приходит с функцией и сроком, но без людей и содержимого — это и называет постановщик. |
| Порядок колонок доски | `BOARD` в `TasksBoard.jsx` | Бэклог → Дедлайн → В работе → Проверка → Готово. Колонка собирает свои состояния (`states`), и в «Бэклоге» их два — карточка называет своё словом. «Бэклог» — лежит и ждёт очереди, «Дедлайн» — взято на срок, и срок горит. Стрелка «›» доводит только до «Проверки»: «Готово» ставит проверяющий, принимая отчёт. |
| Люди актива и их порядок | `crewOf` / `crewRank` / `byCrew` + `entities[].crew` | Роли отвечают на вопрос «кто чем занят», список людей — на другой: «кто это вообще». Один человек с тремя ролями в нём один раз. Порядок задаёт владелец руками, и в этом же порядке людей предлагают в формах выбора: у выбирающего бывают причины, которых в цифрах нет. Хранится только ПОРЯДОК — членство по-прежнему за ролями, иначе удаление из роли пришлось бы повторять во втором списке. А сами роли всегда сортируются по рейтингу (`byRating`): там вопрос «кому поручить», и переключателя вида больше нет. |
| Камера схемы | `SchemeSVG` в `SystemModel.jsx` (`forwardRef`: `zoomBy`, `fit`, `zoom`; `cam` ref `{x,y,z}`, `size`, `fitZoom`, `ZOOM_MAX`, `settle`, `zoomAt`, `panBy`, `animateZoom`; `data-scheme-box`) | Владелец (2026-09-14): схема в окне фиксированной высоты (`min(56vh, 520px)`, `overflow: hidden`, `touch-action: none`), лист внутри через `viewBox` — страница от масштаба не двигается. Камера живёт в ref и применяется к svg напрямую на каждое движение (`setAttribute("viewBox")`), React перерисовывает по концу жеста (`bump`) и берёт ту же камеру. Щипок — вокруг точки между пальцами: при старте запоминается точка листа под серединой, на каждом движении она ставится под нынешнюю середину. Минимум — вписать лист (`fitZoom = min(w/CW, h/CH)`), максимум 2,5; лист меньше окна — по центру; за край листа — не дальше половины окна. Один палец/мышь по пустому месту — прокрутка; колесо — прокрутка, с Ctrl — масштаб у курсора; «−»/«+» — вокруг центра с переходом 160 мс по `performance.now()` (аргумент кадра в jsdom считает от другой точки). Размер окна — `ResizeObserver`, без измерения — 1000×600 (тесты). Формы над схемой — одной строкой без переноса, одной высоты, под ползунком подписи нет. Полоска состояния на блоке — внутри, зелёная/красная по `assetOk`. |
| Участники без актива | `LooseCrew.jsx` (`looseOf`, `rolesOfAsset`), `data-entity` у блока актива в `SchemeSVG` | Человек вступил, получил роль — и не попал ни в один актив: работы у него нет вовсе (задачи живут у функций, функции у активов), а заметить это можно было, только обойдя все активы по очереди. Подложка над схемой ПОЯВЛЯЕТСЯ, когда такие есть, и исчезает, когда не осталось ни одного: пустой блок «без актива: 0» — мебель. Нажатие раскрывает облако мини-блоков (имя и роли), оттуда человека ПЕРЕТАСКИВАЮТ на актив. Жест указателем (`pointer*`), тот же, что двигает блоки схемы: HTML5 drag-and-drop на телефоне не работает вовсе. Куда отпустили — спрашивается у документа (`elementFromPoint` → `data-entity`), а не у ближайшего блока. Актив берёт человека, только если хоть одна его роль названа у какой-нибудь ЕГО функции: иначе работы там для него нет, и отметка в воркерах была бы обещанием, которого актив не выполнит. Не сходится — сказано словами, какие роли актив спрашивает и какие у человека есть; отпустили мимо активов — просто ничего не случилось. |
| Роль и договор | `org.roles[] = {id, name, tabs, contract}`, `org.users[] = {roles[], contracts{}, pending}`, `registerUser`/`openRoles` (`server/src/lib/orgStore.js`), `RegisterPanel.jsx` | Участником человек становится не потому, что его добавили, а потому, что ПОДПИСАЛ ДОГОВОР: акцепт — сам договор, а не чужое нажатие. Договор у РОЛИ, под каждую свой; шаблон кладёт владелец («Люди и роли»), подписанный экземпляр приносит человек — и система сама выдаёт ему эту роль. Роль без шаблона подписывать нечем, она выдаётся сразу, и так и написано рядом: молчание читалось бы как «договор есть, просто не показали». Позвал владелец — роль ЖДЁТ договора (`pending`), и приложение ведёт человека подписывать её, а не показывает пустые вкладки. Регистрация — весь путь на одном экране: роль → договор → подписанный экземпляр → «Вступить»; список ролей для неё открыт всякому вошедшему (только имена и договоры — в организацию вступают, а не заглядывают), а подписанный файл едет строкой base64 в сам маршрут регистрации: общее хранилище файлов заперто для участников, и открывать его ради регистрации значило бы раздать диск всем подряд. | 
| Роли — ОДИН список | `org.roles`, `users[].roles[]`, `funcs[].posts`, `eligible`/`byPost`/`dutyOf` (`lib/funcs.js`) | Прежде списков было два: роли (что человеку показывать) и должности (кем он числится). Вопрос один — кто он здесь, — и список теперь один: те же роли открывают вкладки, по ним заключают договоры, ими названы роли у функций и отмечены воркеры. Ролей у человека НЕСКОЛЬКО: он и дизайнер, и проверяющий, — поэтому отметки, а не выпадающий список (тот заставлял выбрать одну и молча снимал остальные), а вкладки складываются объединением. Работу берёт тот, у кого есть ХОТЬ ОДНА названная у функции роль: требовать совпадения всех значило бы не пускать того, кто умеет больше. В списке воркеров актива стоят только люди С РОЛЬЮ — работу назначают ролью, и строка у безролевого обещала бы то, чего не будет; уже отмеченные остаются всегда. Должности прежних записей читаются как роли без вкладок — никто не теряет ни доступа, ни своего дела. |
| Анкета человека | `users[].answers` (вопросы — из анкеты роли, см. «Анкеты как словари»), `users[].about` остаётся в данных (`PROFILE_FIELDS`), `PUT /api/org/me/profile`, вкладка «Анкета» (`ProfilePanel`) | О чём спрашивать, решает анкета, назначенная роли: вопросы — её, ответы — человека. Ролям без анкеты спрашивать нечего — и формы «моя анкета» нет вовсе, а не пустая (2026-09-13; прежнее одно поле «о себе» с экрана ушло, в данных осталось и читается как «заполнена ли анкета»). Форма стоит ПЕРВОЙ на вкладке, под ней имя шапкой, график, задачи и работы; вопросы — в своей области прокрутки (`maxHeight: 55vh`), чтобы длинная анкета не уносила график. Пишет сам человек и только свою: заполненная кем-то другим, она была бы чужим мнением под чужим именем, — поэтому маршрут открыт всем позванным, а не владельцу. Лежит рядом с человеком в организации, а не в модели. Вкладка стоит ПЕРВОЙ и открыта всем вошедшим. Чужого человека открывают ОКНОМ по нажатию в списке воркеров (`Modal` в `SystemModel`): вкладка уносила бы со схемы, которую он сейчас собирает, и вернуться было бы некуда. Списка чужих анкет на странице нет — она про одного человека. График и статус — переключатели: сохраняются сами при каждом нажатии (с короткой задержкой, уход со вкладки отправляет сразу; запросы по цепочке, чтобы не обогнать друг друга), кнопка «Сохранить анкету» — для текста. Черновик собирается из `me.profile` (своя) или из списка людей (чужая), но тронутые человеком поля (`touched`) переживают приход нового списка: прежде каждый такой приход пересобирал черновик из пропсов, и нажатое до него молча заменялось прежним — так и выглядело «график не сохраняется». Рядом в анкете — `warnMin`, за сколько минут предупреждать о задаче: настройка человека, а не задачи (карточка «Напоминания» в инструментах, `RemindersCard`); `SystemModel` подставляет его в `warn` каждой задачи перед `syncSchedule` и шлёт расписание при входе (`settled`: известно, кто вошёл, а у владельца — и что открыто), не только при правке. |
| Анкеты как словари | `org.forms[]`, `roles[].form`, `users[].answers` в `org.json` (`addForm`/`setForm`/`removeForm`/`setRoleForm`/`formsFor` в `orgStore.js`), маршруты `/api/org/forms` и `/api/org/roles/:id/form`, раздел «анкеты» и выбор «анкета:» у роли в «Людях и ролях» (`FormsPanel.jsx`), вопросы в анкете человека (`FormAnswers` в `ProfilePanel`) | Анкета — СЛОВАРЬ вопросов `{id, name, questions: [{id, text}]}`, и назначается РОЛИ, как договор: о чём спрашивать человека, решает то, кем он здесь является. Анкеты человека — объединение анкет его ролей, каждая один раз; их отдаёт «кто я» (`forms`) и список людей владельца (у каждого человека `forms`), чтобы чужая анкета читалась вопросом и ответом. Ответы лежат у человека по идентификатору вопроса (`answers`), а не по тексту: идентификатор выдаётся один раз при добавлении и переживает правку текста; удалённый вопрос свой уносит, и новый с тем же текстом ответа не наследует. Ответы едут тем же `PUT /api/org/me/profile` и ложатся поверх прежних — форма шлёт то, что видит, и ответ на вопрос другой роли не пропадает. Есть анкеты по ролям — человек видит вопросы (заголовок анкеты, вопрос подписью, поле ответа) и сохраняет их той же кнопкой; нет — прежнее одно поле `about`. Старые записи: нет `forms` — пусто, нет `answers` — пусто, `about` на месте. Удаление анкеты снимает её с ролей, ответы остаются. Регистрация анкету не спрашивает. «Загрузить анкету» (2026-09-13) — пронумерованный список вопросов, вставленный или из `.txt`: `parseNumbered` в `lib/formText.js` («N. текст» — вопрос, строка без номера продолжает предыдущий, без номеров — отказ словами), уезжает одной записью `POST /api/org/forms` с `questions`. |
| Выполняемые задачи человека | `dutyOf` (`web/src/lib/funcs.js`), `dutyFor`/`refuseFunc` (`server/src/lib/workspaceStore.js`), `GET /api/workspace/duty`, `POST /api/workspace/funcs/:id/duty`, раздел в `ProfilePanel` | То же, что в воркерах актива, но собранное по ВСЕМ активам сразу: настройки актива открывает владелец, а знать, что на нём висит, должен и позванный. Правило одно и то же с обеих сторон: человек — воркер актива (`crewOf`) И его выбрали ДОЛЖНОСТЬЮ у функции (`byPost`); должность приходит в «кто я» (`identify` отдаёт `position`). Считают двое, потому что видят разное: владельцу модель видна целиком — он считает список у себя; позванному она не видна — за него считает сервер. Выбрать себе работу здесь НЕЛЬЗЯ: поручает должность, а раздел только показывает и даёт отказаться. Отказ — то же `funcs[].except`, что владелец ставит в карточке воркера: одна запись на один смысл, и отказ виден в обоих местах. Отказаться можно только от того, что поручено — иначе список исключений заполнился бы посторонними; берут назад тем же нажатием, отказ не окончателен. Стоит ПЕРЕД «комментариями и работами»: сначала то, что на человеке сейчас, потом то, как он работал раньше. |
| Рабочий график и статус | `users[].days/from/to/status` на сервере (`WORK_STATUSES`, `scheduleOfPerson`, `scheduleText` в `lib/workers.js`) | Анкета говорит, ЧТО человек умеет; график и статус — РАБОТАЕТ ЛИ ОН СЕЙЧАС, и это спрашивают раньше: ставить задачу тому, у кого сегодня выходной, значит назначить срок, которого никто не обещал. Две разные вещи: **график** постоянный (дни недели и часы «с — до»), **статус** сиюминутный (готов взять задачу / короткий перерыв / сегодня не работаю / не готов брать задачи) — и одно другого не отменяет. Пишет их сам человек, тем же маршрутом и тем же нажатием, что и анкету; читаются они кем угодно — стоят первыми в окне человека и коротко в списке воркеров. Пусто — это «не названо», а не «все дни и круглые сутки»: дописать за человека семидневку нельзя. Сервер разбирает присланное (`0–6`, «ЧЧ:ММ», один из четырёх статусов) — «понедельник» или «25:00» в записи означали бы график, по которому нельзя сказать ничего. Статус на экране — ПО ГРАФИКУ с приоритетом выбора (`inWorkTime`/`lastBoundary`/`liveStatus` в `lib/workers.js`, 2026-09-13): границы графика — начало и конец рабочего времени — меняют статус сами («на рабочем месте» / «сегодня не работаю»); выбор человека (`statusAt` — момент нажатия, из приложения или сервером при смене статуса) приоритетнее графика до следующей границы после него, потом снова действует график. Без дней в графике — нажатое. Считается на показе (анкета, строка воркера в карточке актива); в записи — нажатое и момент. Карточка графика — две формы внутри: «статус» и «рабочие дни и часы». |
| Часы отдельного дня | `users[].perDay` на сервере (`perDayOf` в `orgStore.js`), `perDayOf`/`dayHours`/`scheduleText` в `lib/workers.js`, правка в `Schedule` (`ProfilePanel.jsx`) | Исключения из общих «с — до»: `{ [день 0–6]: {from, to} }`. Нет записи — действуют общие часы; запись дня — целиком, без единого часа её нет, у выходного — тоже. Правка (2026-09-13): ДВОЙНОЕ нажатие по дню открывает правку и берёт его в набор (жёлтый) — любой день, и выходной тоже; в правке следующие дни берутся и убираются ОДНИМ нажатием; галочка «рабочие дни» включает или выключает дни набора, поля «с»/«до» активны только при ней, пишут в записи всех дней набора и показывают часы первого; «Принять» закрывает правку. Вне правки одиночное нажатие — только показ: поля с часами этого дня, отключённые («выходной» — словами); повторное снимает показ. Без правки и показа поля пишут общие часы. Набор и показ — состояние экрана, на сервер не едут. Текст: дни со своими часами — своей группой через «;». |
| Кнопки под уведомлением | `taskKeyboard`/`onTaskButton`/`onTaskMessage` в `lib/botTasks.js`, `keyboardFor` в `lib/scheduler.js`, `takeTask`/`deferTask`/`taskFor` в `workspaceStore.js` | Уведомление о НАЧАЛЕ работы приходит с двумя кнопками — «🔴 Отложить» слева, «🟢 Начать» справа (отказ слева, действие справа; красить инлайн-кнопки Telegram нельзя, цвет несёт эмодзи). «Отложить» спрашивает, НА СКОЛЬКО, — одним экраном кнопок (`DEFER_CHOICES`: 15 мин, 30 мин, 1 ч, 2 ч, 4 ч, 1 день и «Назад»; `TASK_DEFER_FOR`/`TASK_DEFER_BACK`, для постановки `SETUP_DEFER_FOR`/`SETUP_DEFER_BACK`), а не берёт срок из анкеты: поле «откладывать на» убрано (2026-09-13), `deferMin` в данных больше не читается. Задача остаётся в бэклоге отложенной, а в момент `deferredUntil` планировщик присылает уведомление заново. «Начать» переводит задачу в работу, и то же сообщение становится «Сдать отчёт»: по каждой обязательной вещи (`requiredGives`, повторено на сервере) присылается файл, потом часы, текст, оценка постановки и комментарий — скрытый или публичный; без обязательной вещи «Отправить отчёт» не появляется. Нажатие двигает задачу на общем складе работы — там же, где её двигает нажатие на доске, чтобы два места не разошлись в правилах. Отвечают кнопки ВСЕМ позванным, а не одному владельцу, но ПРИХОДЯТ только исполнителю: владелец, постановщик и проверяющий получают то же уведомление без кнопок — у них «Начать» отвечало бы «не ваша». Кнопки стоят и под предупреждением «через N минут», и под «пора начинать» (v1.2): в момент предупреждения человек и решает, успевает ли, — «Отложить» значит «в назначенный час не начну, напомни позже». Запись расписания БЕЗ поля исполнителя (сохранена до v1.1, `assignee === undefined`) считается записью того, у кого лежит, — иначе старые напоминания приходили без кнопок; «никому» (`null`) — по-прежнему без кнопок. «Отложить» срок НЕ двигает: срок ставит постановщик, и менять его нажатием исполнителя значило бы переписывать договорённость в одну сторону; и «Отложить» отказывает сразу, если задача уже в работе или сдана, — не после вопросов про часы. «Отложить» под предупреждением гасит и плановые «через N минут»/«начинается», лежащие раньше `deferredUntil` (`dueNotifications`): иначе в назначенный час приходило бы «Начинается» с кнопками вопреки «когда время выйдет, напомню снова». Нажатие на сообщении, пересланном в ГРУППУ (Telegram сохраняет клавиатуру при пересылке), получает только ответ на само нажатие — «Кнопки работают только в личном чате с ботом» — и ничего в чат; вторым рубежом `targetOf` в `botTasks.js` и `onAssistantButton` всегда отвечают в личный чат нажавшего (`from.id`) и правят сообщение, только если оно оттуда же. Состояние шага — на диске (`bot-steps.json` в `WORKSPACE_DIR`, запись tmp+rename, чтение при старте): перезапуск его не сбрасывает; «не помню, с чего начали» бот говорит только на кнопку, шага у которой нет. |
| Таймлайн файлом | `lib/timelineDoc.js` (`barOf`, `timelineHtml`) + кнопка в `Timeline.jsx` | Экран показывает модель СЕЙЧАС, а спрашивают «как оно шло»: модель меняется, и вчерашняя картина исчезает бесследно. Поэтому таймлайн, как и отчёт, ложится HTML-файлом через то же хранилище (`putReportFile` + `deliverReport`): открывается везде, печатается в PDF, через год читается тем же текстом, ни скриптов, ни ссылок наружу. В файл идут ВСЕ задачи, а не отфильтрованные на экране, и вместе с историей — когда отложили, когда взяли, что сдали и как приняли. Собирается чьими-то глазами (`viewer` = `me.id`), по тому же правилу, что срез сервера (`taskViewFor`) и доска (`canSeeComment`): отметки в файле нет никогда (без имени она всё равно называет единственного проверяющего, а исполнитель своих оценок не видит), скрытые слова решения — только автору и исполнителю-адресату. Без этого файл владельца, у которого модель целиком, уносил бы наружу скрытые слова всех проверяющих. `barOf` живёт в библиотеке, а не в компоненте: по нему рисуется и экран, и файл, и правило у них обязано быть одно. |
| Фактор ждёт полную порцию | `runSide`, ветка `isFactor` | Он заранее знает, сколько ему нужно на одно срабатывание, и берёт только когда это количество появилось; на неполную порцию не срабатывает, а ресурс копится до следующей попытки. Задача так не может — работу делят и делают частями, — а фактор либо случился целиком, либо не случился вовсе. |
| Сколько функция выполнится на самом деле | `goalRuns()` в `lib/goals.js` → `plan` в `runSide`/`load`/`transfers` | Из ПРИМЕНЁННЫХ целей: `perMonth` (цель с темпом) повторяется каждый месяц, `once` (разовая) — запас работы, который тратится и кончается. Больше потолка не выйдет. Ни одной применённой цели — ничего и не происходит. |
| Что нужно сделать ради цели | `solve()` / `solveRange()` в `lib/plan.js` | Разворачивание от цели назад: чтобы получить ресурс, нужны выполнения функции, которая его выдаёт; на них уйдут её входы, а входы произведут другие функции. Остатки идут в дело первыми. Два ответа — по нижней и по верхней границе вилок. |
| Сколько это займёт | `workHours` / `criticalHours` в результате `solve()` | Два времени: сумма работы (человеко-часы) и календарный срок по самой длинной цепочке зависимостей — функция, ждущая чужой выход, раньше него не начнётся. |
| Сколько раз функция срабатывает за месяц | `cycles()` в `lib/plan.js` | `месяц / время выполнения`. Цикл в неделю — примерно четыре раза, цикл в полгода — одна шестая: результат, на который ушло полгода, не может появиться разом. |
| Прогноз как лента | `runSide()` / `forecast()` в `lib/plan.js` | Две границы: нижняя — выдали по минимуму, взяли по максимуму; верхняя — наоборот. Одна линия здесь была бы обманом: она выглядела бы как знание, которого нет. |
| Ограниченный ресурс, за который конкурируют | `runSide()`, доля `share` | Взять больше, чем есть, нельзя. Если спрос за месяц больше остатка, все, кто берёт, получают **пропорционально запросу** — не «кто раньше в списке»: порядок функций это случайность заведения, а не приоритет. |
| Цель (референсный режим) | `traits[].want` + `reach()` | Два срока, а не один: «наверняка» — по нижней границе, «в лучшем случае» — по верхней. Вилка одной даты не даёт. |
| Задача = выполнение функции | `tasks[].funcId` (`TasksBoard.jsx`) | Задача не несёт собственных величин: что берётся и выдаётся — свойства функции. Функция **не выбирается в задаче**: её задаёт то, под чем нажата кнопка «+ выполнение». |
| План и факт | вилки функции (план) vs `tasks[].submissions[]` (факт) | Сдача записывает, что вышло на самом деле: сколько часов ушло и сколько каждого ресурса взяли и выдали. Из **принятых** сдач считается среднее арифметическое (`runsOfFunc`, `runHours`, `runQty`), и оно вытесняет вилку в расчёте. |
| Приём отчёта | `ReviewBoard.jsx` + `/review` | «Готово» ставит проверяющий, а не исполнитель: непринятая сдача — заявление о себе, а не измерение, и в расчёт она не идёт. Решение целиком (кто, когда, принял ли, оценка, слова) ложится в `tasks[].reviews[]` — из него и растёт история человека. |
| Нагрузка человека | `load()` в `lib/plan.js` | Часы функций, где он исполнитель, поделённые между исполнителями поровну. Число исполнителей на скорость функции не влияет — см. инвариант 6. |
| Стрелка передачи ведёт к функции | `onOpenFunc` в `SchemeSVG` → `focus` в `AssetPanel` | Стрелку рисует функция — по стрелке до неё и надо доходить. Просьба открыть приходит с меткой `n`, иначе повторное нажатие на ту же стрелку ничего бы не сделало: значение не изменилось. |
| Передачи на схеме | `transfers()` + `SchemeSVG` | Выход ресурсом чужого актива превращается в месячные числа: сколько ресурса уезжает из актива. Каждая передача идёт своей полосой — встречные иначе прячут друг друга. Подпись лежит НА своей линии (полосы разведены на высоту таблички, а несколько передач между теми же активами делят линию поровну): отодвинутая подпись висит сама по себе, и к какой стрелке она относится, приходится угадывать. |
| Проверки строения | `checkAsset` / `checkFunc` / `checkTrait` в `lib/funcs.js` | Подписи «актив», «функция», «ресурс» белые, пока строение сходится, и красные, когда нет; рядом «?» с определением словами. Проверка структурная и от времени не зависит. |
| Классификация ресурсов | `kinds` (`KINDS0`) | Значок, название, цвет. Живёт на вкладке «Ресурсы» актива, под спойлером: классификация — свойство ресурса, и держать её в другом месте значило бы разложить одно понятие по двум. |
| Напоминания о работе | `server/src/lib/scheduler.js` + `/api/schedule` | Планировщик раз в минуту смотрит расписания и шлёт сообщения ботом. Расписание строится из задач: время — `start` задачи (у функции есть длительность, но нет календаря); «за сколько предупредить» — НЕ поле задачи, а настройка человека, которому напоминают (`warnMin` в анкете, карточка «Напоминания» в инструментах), которую отправитель подставляет в `warn` каждой задачи перед `syncSchedule`. Расписание у каждого своё и шлётся при входе и при правке. |
| Файл отчёта | `server/src/lib/reportStore.js` + `putReportFile()` | Файл лежит на диске сервера, в сценарии — ссылка `/api/reports/<scope>/<id>`. Без сервера — инлайн `data:`-URL внутри сценария; показывать надо тот вид, который есть: `reportSrc()`. |
| Кто смотрит на модель | `server/src/lib/orgStore.js` + `web/src/identity.js` | Владелец один (`OWNER_TELEGRAM_ID`, иначе первый вошедший). Роль — **набор вкладок**, а не уровень доступа. Без сервера приложение одиночное. |
| Обновление у позванного | `applyWorkspace`/`pullNow`/`POLL_MS` в `SystemModel.jsx` | Позванный переспрашивает свой срез (`GET /api/workspace`) раз в полминуты, пока вкладка на виду, сразу — когда она снова на виду, и после каждого своего действия: ответ сервера и есть правда, доска показывает его, а не своё. Тот же ответ ничего не перерисовывает. Владелец не переспрашивает: его модель живёт в его окне и уезжает на сервер сама (`putWorkspace`), класть серверную поверх его правок нельзя. Черновик и «Восстановить?» — только владельцу и в одиночном режиме: у позванного правда на сервере. Срез (`viewFor`) несёт `procs` тех процессов, чьи функции в нём, — иначе функция гипотезы читалась бы как принятая. |
| Рейтинг в постановке | `statOf` в `TaskSetup` (`TasksBoard.jsx`), `ratings` из `SystemModel` через `ReviewBoard` | Подпись рядом с именем в выпадающем списке — глазами сервера (`GET /ratings`), если он ответил про человека: средняя и число оценок оттуда, «в срок» и число работ — по видимым задачам; про себя — «свой рейтинг скрыт». То же, что показывает анкета (`PersonStats`): два экрана не должны называть одному человеку разные числа. |
| Что кому видно | `server/src/lib/workspaceStore.js` (`viewFor`) | Не-владельцу отдаются только его задачи и ровно то, на что они ссылаются: их функции, ресурсы этих функций и их активы. Фильтрует **сервер**: спрятать чужое в интерфейсе значит не спрятать его вовсе. |
| Один файл модели, много писателей | `withModel`/`writeModel` в `server/src/lib/workspaceStore.js` | Модель — один файл `data/workspace/model.json`, а пишут его владелец (целиком), постановщик (постановка своей задачи), исполнитель («взять», «отложить», сдача), проверяющий (приём), участники (комментарии) и планировщик (публикация оценок). Каждая такая правка — «прочитать → поменять → записать», поэтому все они идут через одну очередь в процессе (`withModel`, цепочка обещаний, как `inOrder` в `memoryStore.js`): два нажатия в одну миллисекунду иначе затирали бы друг друга молча. Запись — через временный файл и переименование: обрыв на середине не оставляет обрезанного файла, из которого чтение вернуло бы пустую модель. Процесс один, поэтому файла-замка нет; второй процесс на том же файле эту защиту не унаследует. |
| Приглашение людей | `server/src/lib/bot.js` | Владелец пересылает боту сообщение от человека, бот показывает роли кнопками либо заводит новую по названию. Логика — чистая функция `handleUpdate(update, deps)`. Сообщение из группы (`chat.type` group/supergroup, включая `edited_message`) бот ЗАПИСЫВАЕТ (`deps.chats.record` → `lib/chatStore.js`) и не отвечает ничем: чат общий, а бот отвечает каждому про своё; без записи — тоже молчит. |
| Совещание | `server/src/lib/callStore.js` + `CallApp.jsx`, `CallRoom.jsx` | Встреча — время, текст и комната до 20 человек. Звонок — отдельное окно (`/call`), медиа только через сервер (`iceTransportPolicy: relay`). Ссылка на встречу и есть право войти. |
| Расшифровка записи звонка | `server/src/lib/transcribe.js` (`transcribeFile`, `transcribeRecording`) + `putTranscript`/`listTranscripts` в `callStore.js` (`data/calls/transcripts.json`, копия текста у встречи в `transcripts[]`) | Расшифровка — такая же задача для модели, как ответ на вопрос: строка «расшифровка записей звонков» в таблице «задача → модель» того, кто запись сохранил (`modelFor(userId, 'transcribe', {fallback: false})`); свой ключ, свой счёт. Строка пуста — расшифровки НЕТ, без отката на модель чата: та записи не расшифровывает, и вместо «выберите модель» человек читал бы «провайдер ответил 400». Умеет только провайдер вида «OpenAI» (`POST /audio/transcriptions`, multipart, `language: ru`), про остальные сказано словами. Предел провайдера — `MAX_TRANSCRIBE_BYTES` = 25 МБ (столько принимает `/audio/transcriptions` у OpenAI и Groq; при 300 кбит/с это ~10 минут записи): запись больше не отправляется, причина ложится словами. Запускается маршрутом сохранения записи (`routes/reports.js`, kind `call`) ПОСЛЕ ответа и в фоне: текст делается минуты, а «запись сохранена» человек ждёт сейчас. Ключ — id файла записи, потому что запись бывает и без встречи; встреча — из заголовка `X-Report-Meeting`, если есть. Состояние — словами: модель не выбрана (текста нет и не будет, пока не выберут), идёт, готово, не удалось и почему. Выбор модели в строке «расшифровка» (`PUT /tasks`) дорасшифровывает в фоне записи без текста (`retranscribeFor`: модели не было, не удалось, «идёт» оборвалось), по одной — запись до 100 МБ в памяти. «Идёт» пережить перезапуск не может (ждал её процесс, которого нет): при старте `resumeTranscripts` в `index.js` перезапускает все «идёт» из байтов `ownReport`, а без файла пишет «прервана перезапуском сервера»; «идёт» старше `TRANSCRIBE_TIMEOUT_MS` контекст называет прерванной, а не идущей. Удаление записи уносит и текст. Копия текста у встречи наружу не уходит: `GET/POST /api/calls` срезают `transcripts` (запись с чужим `meetingId` может сохранить любой позванный, а список встреч читают создатель и владелец). |
| Чаты групп | `server/src/lib/chatStore.js` (`recordGroupMessage`, `chatsFor`; `data/chats/<chatId>.jsonl` + `.meta.json`) + `telegram.getChatMember` | Бот хранит ВСЁ, что получает в группах, без срока (Telegram истории не отдаёт; privacy mode у бота выключен — DEPLOYMENT.md 6.7). Строка на сообщение, правка — новая строка с тем же id: сказанное остаётся, при чтении побеждает последняя. Кому что видно, решается при КАЖДОМ обращении, а не при записи: в контекст попадают только чаты, где человек состоит прямо сейчас по слову Bot API; ошибка проверки = «не состоит», как и молчание Telegram дольше `CHAT_MEMBER_TIMEOUT_MS` (5 с, `AbortSignal.timeout`): очередь вопросов одна на всех, и без предела молчащий api.telegram.org держал бы её до 5 минут на стадии «собираю данные». Чаты спрашиваются параллельно, ждём самый долгий, а не сумму. Предел 20 000 знаков общий на все чаты, берётся новое; сколько не вошло — числом. |
| Операции (выражения) | `web/src/lib/expr.js` (`parseExpr`: числа, `@{id}`, `+ − * /`, скобки, `%`; `evalExpr`, `toStored`/`toShown`), `ExprField.jsx` (`plain` — без сравнения), `PortQty` в `AssetPanel.jsx` (`expr` порта), `splitQty`/`marksOf` в `lib/process.js`, `Backdrop` в `ProcessPanel.jsx` | Один язык для целей, портов функции и количеств техпроцесса (владелец, 2026-09-15: «это операции, добавь процент от значения»). Процент: «20%» = 0,2; процент, за которым сразу число/ресурс/скобка, умножается на него («20% @Заявки»); «@X*20%» — то же. Порт: переключатель «операция» открывает поле; результат по нынешним остаткам ложится в `lo = hi`, выражение хранится в `p.expr`. Техпроцесс: «ресурс 20% @спрос» — `qty` посчитан, `expr` остаётся в строке (`formatStep`), непосчитанное — `exprError` и ⚠. Красные метки в поле — подложка `Backdrop` с тем же текстом/шрифтом/отступами: `span` слов из `parseLine`, состояние из `stateOf` (с памятью записи через `resolveProc`); ошибка строки — абсолютной меткой у правого края, переносы не трогает. Ошибки форм «Ролей» — на месте: `act(fn, {quiet})` пробрасывает отказ форме. |
| Договоры и подписи | `server/src/lib/docx.js` (jszip: `buildDocx`, `docxText`, `placeholdersOf`/`PLACEHOLDER_RE`, `fillDocx`, `signatureFrames`, `insertSignature`, `docxToHtml`, `htmlToDocx`), `lib/contractStore.js` (`org.docs[]` с `versions[]`, `org.agreements[]`; `addDoc`/`addVersion`/`updateDoc`/`removeDoc`/`removeVersion`/`docHtml`, `setRoleDoc`, `createAgreement` → `inviteLink`, `claimAgreement`/`bindLatestAgreement`, `agreementHtml`, `signAgreement`, `cleanSignature`), `orgStore.roleActive`/`agreementInForce`, маршруты в `routes/org.js`, бот `/start agr_<токен>`; клиент `ContractsPanel.jsx` (`DocsSection`, `DocViewer`, `PlaceholderFields`, `InviteModal`), `SignaturePad.jsx` + `lib/signature.js`, `FramedField.jsx`, `RegisterPanel.AgreementSign` | Владелец (2026-09-14). ДОКУМЕНТ — договор Word с историей версий: каждая версия — файл в хранилище отчётов (`kind: "doc"`), описание изменений, плейсхолдеры `[(ключ): описание]` из текста и индексы рамок подписей (сплошная — сторона 1, пунктирная — сторона 2); последняя версия не удаляется; `values` — значения, заполненные владельцем заранее. Обязательные плейсхолдеры `sum`/`start`/`end` — без них документ не принимается. Просмотр/правка — HTML ↔ docx; при правке через HTML `base` — предыдущая версия (стили, media остаются). Роль → `role.doc`; прежний `role.contract` (файл) читается как раньше. СОГЛАШЕНИЕ — выдача одному человеку: версия, роль, значения владельца, сумма, даты, подпись стороны 1, токен; `to` — кому (по ссылке или по пересылке боту), `userValues`, подпись стороны 2, готовый файл (`fillDocx` + две подписи PNG в рамки). Статусы `sent → signed | revoked`. ПОДПИСЬ — штрихи `{x,y,t,p}`, размер, устройство, момент, `docHash`, клиентский `hash` и `serverHash`; штрихи наружу списком не уходят. `identify`: вкладки — по ДЕЙСТВУЮЩИМ ролям (`roleActive`: соглашение `signed` со сроком, включающим сегодня; роль без документа — как прежде); `inactive[]` — роли с причиной; `agreement` — ждущее подписи соглашение с оставшимися плейсхолдерами. Приглашение: `https://t.me/<бот>?start=agr_<токен>`; «Выбрать чат» — `t.me/share/url`. FramedField — `fieldset`/`legend`: рамка замыкается на середине букв подписи. Изменения документа — не словами, а разницей по абзацам (`web/src/lib/docdiff.js`: `linesOf`, `diffLines`, `diffHtml`, `diffText`) двумя рамками `DiffForms`: «+» зелёная (добавлено), «−» красная (убрано); с правками — правка против последней версии, без — последняя против предыдущей; в дереве версий «+N −M». Черновик: автозагрузка схемы при старте плашку «Восстановить» не гасит; гасит первое собственное изменение (сравнение с документом последней загрузки, `loadGen`) или ответ на неё. |
| Рынок услуг | `server/src/lib/marketStore.js` (`market.json`: `orders[]`, `services[]`; `addOrder`/`updateOrder`/`removeOrder`, `addService`/…, `addOffer`, `addChat`, `setBrief`, `acceptBrief` → `taskOf`, `addDelivery`, `viewFor`/`orderViewFor`), `routes/market.js` (`/api/market`), клиент `web/src/market.js`, правила `web/src/lib/market.js` (`serviceFromFunc`/`orderFromFunc`, `matchServices`), вкладка `MarketPanel.jsx` (`MARKET_TAB` первой в `TAB_LIST`) | Владелец (2026-09-13): первая вкладка перед анкетой, внутри «Заказы» и «Услуги»; заказ оставляет ЛЮБОЙ зарегистрированный (роль не спрашивается — как у анкеты); заказ: название, содержание, стоимость, предоставляемые ресурсы; услуга: название, описание, берёт, выдаёт, за сколько дней. Из настроек функции — «Сделать заказ»/«Сделать услугой» (`onMarket` у `AssetPanel` → `marketDraft` в `SystemModel` → форма с заполненными словами функции: имя, описание, порты по именам ресурсов, срок в днях; дальше запись живёт сама и правится автором). Подходящие услуги — в форме заказа по общим словам (`matchScore`; одна `funcId` важнее слов), одну можно выбрать (`serviceId`). ОТКЛИК виден двоим — автору заказа и своему автору (`canSeeOffer`, режет сервер; остальным — только `offerCount`); один отклик на человека, второй правит первый; открыть отклик — открыть чат (`chat[]` внутри отклика). БРИФ — одна запись на отклик: `{by, rev, gives[], gets, days, note}`; пишет любая сторона («Договорились» / «Изменить»), кто последний правил — тот `by`; «Есть предложение» у обоих: жёлтая (`data-tone="theirs"`) — писала другая сторона, можно принять; голубая — ваше, ждёте. Принять может только не-автор (`acceptBrief`); сделка: `order.status="deal"`, отклик `accepted`, в модели владельца заводится задача (`taskOf`: постановщик и проверяющий — заказчик, исполнитель — откликнувшийся, `funcId` из услуги или заказа, сразу `backlog`, срок по брифу `endBy:"hand"`, `market:{orderId,offerId}`) — исполнитель видит её на «Задачах» (assignee), заказчик на «Проверке» (reviewer/setter). Что заказчик отдаёт — `deliveries[]` после сделки: текстом здесь же или файлом через хранилище отчётов (`kind:"market"`). Рынок один на сервер и общий; без сервера вкладка объясняет, почему пуста. Опрос раз в полминуты и после каждого действия — как срез позванного. |
| Помощник | `server/src/lib/assistantContext.js` (`contextFor`), `assistantQueue.js`, `routes/assistant.js`, `botAssistant.js`, `web/src/assistant.js` | Не новый источник правды, а второй способ спросить ту же модель словами. Своей границы видимости у него нет: контекст собирается теми же функциями, что и экраны (`viewFor` — задачи и ресурсы, `listReports` — файлы, `listMeetings` — встречи, `readSchedule` — напоминания, память), владельцу — модель целиком, и собирается ЗАНОВО на каждый вопрос. Знает он всё, что доступно спросившему: модель с посчитанными остатками, САМИ ВЕЩИ (`describeMaterials`: единицы ресурсов с номерами, содержимым и тем, из сдачи они или из «Материалов» — по ресурсам, которые человек видит), задачи со сдачами, оценками и комментариями, файлы, встречи, записи звонков, чаты, память и РАСПИСАНИЕ (`describeSchedule`: что и когда пришлёт бот, включая висящие напоминания). Про других людей — ничего; имена рядом с задачей — только те, что человек и так видит на её карточке; своих оценок нет (C4). Прогноз на сервере не считается — контекст говорит об этом сам. Вопрос — в два шага (`POST /ask` → id, `GET /ask/:id`), потому что один длинный запрос рвут nginx и WebView; ответ живёт три минуты в памяти процесса. Чем отвечает — решает `modelFor(userId, task)` из настроек ЧЕЛОВЕКА (`assistantSettings.js`, v1.2): общего ключа нет, каждый спрашивает своей моделью за свой счёт; `task` — строка таблицы «задача → модель» (`chat` по умолчанию, `bot` из чата бота). У каждого вопроса свой `AbortController`: `cancel(id, userId)` ПРЕРЫВАЕТ запрос к провайдеру, а не прячет ответ (иначе человек платил бы за то, что не увидит), и завершает вопрос словами «Отменено» — тем же путём, что и любую ошибку. Стадии `onProgress` ('context' → 'model' → 'answer') очередь сообщает наружу, сама их не показывает. В чате бота отвечает любому позванному, и ответа модели бот НЕ ждёт: сразу ОДНО сообщение-статус, которое ПРАВИТСЯ по стадиям («Собираю ваши данные…», «Спрашиваю OpenAI / gpt-4o-mini…», «Отвечаю…», «Готово»), под ним «✖ Отменить» / «✎ Уточнить» (`ai:cancel:<id>` / `ai:refine:<id>`); ответ — отдельным сообщением, иначе Telegram не показал бы уведомления. «Уточнить»: бот ждёт дополнение (в памяти процесса, как `pending`, и не дольше `KEEP_DONE_MS` = 10 мин — иначе вчерашнее «Уточнить» молча склеивало бы сегодняшний вопрос со вчерашним), текущий запрос отменяется и задаётся новый — `<вопрос>\n\nУточнение: <дополнение>`; ждать ответа, чтобы переспросить, — платить за заведомо не тот ответ; вопрос уже забыт — присланный текст задаётся новым вопросом, а не выбрасывается. Статус «Думаю…» не отправился (429, сеть) — вопрос всё равно в очереди, ответ приходит отдельным сообщением, стадии молчат. У запроса к провайдеру есть предел (90 с), у очереди — свой, и вопрос, до которого очередь не дошла, завершается словами, а не тишиной. «запомни: …» и документ — в память. Чего нет — сказано словами («Задач нет», «Помощник не настроен: добавьте провайдера и ключ в Инструментах → Помощник»). |
| Ключи и модели | `server/src/lib/assistantSettings.js` + `aiProviders.js`, маршруты `/api/assistant/settings`, `/providers`, `/tasks` в `routes/assistant.js`, карточка `AssistantSettings.jsx` | У каждого человека своё, в файле `ASSISTANT_DIR/<userId>.json` (v1.2): провайдеры и таблица «задача → модель». Провайдер — ВИД API (`openai` — совместимый с OpenAI, `anthropic`, `hf` — router в формате OpenAI), адрес (пусто — умолчание вида; OpenRouter/Groq — вид «openai» с другим адресом), ключ и список моделей. Общего ключа нет, владелец за других не ставит; прежние `AI_PROVIDER`/`*_API_KEY` из `.env` переносятся владельцу один раз, когда его файла ещё нет. Наружу ключ не уходит никогда — только `hasKey`: всё, что покидает модуль, проходит через `providerView`. `modelFor(userId, task)` — единственный вход для очереди, бота и расшифровки: своя строка таблицы → строка `chat` → первый провайдер с ключом и первой моделью → `null` и `NOT_CONFIGURED`. Исключение — расшифровка: `modelFor(userId, 'transcribe', {fallback: false})` берёт только свою строку, откат на модель чата ей вреден (см. «Расшифровка записи звонка»); на экране пустая строка «расшифровка» так и подписана — «не выбрана (расшифровки не будет)». Чтение синхронное: файл крошечный, а функция, работающая и с `await`, и без, не заставляет вызывающих угадывать. Строки таблицы, ссылающиеся на удалённого провайдера или пропавшую модель, — `null`, а не след прежнего выбора. Три API спрятаны за `complete()` и `listModels()`; `complete` принимает `signal` — «Отменить» в боте режет сам запрос, а не только кнопки («Запрос отменён»). Ошибки провайдера — словами, с именем провайдера и статусом, ключ из текста вырезан. |
| Память помощника | `server/src/lib/memoryStore.js` (`data/memory/<userId>.json`) + `/api/assistant/memory` | Единственное, что человек кладёт в помощника САМ: заметка или файл. Только своё: файл памяти — на пользователя, id из подписи. Файлы не дублируются — ложатся в хранилище файлов отчётов с `kind: "memory"`, здесь ссылка; текстовый файл читается в запись, у картинки и PDF остаётся имя, и это говорится честно. Пределы названы числом: 200 записей, 20 000 знаков. |
| Агенты | `agents[]` в записи человека (`assistantSettings.js`: `addAgent`/`updateAgent`/`removeAgent`, `BUILTIN_AGENT_ID = "assistant"`), маршруты `/api/assistant/agents`, участник-агент `ag_<id>` в `org.users` (`addAgentUser`/`renameAgentUser`), вкладка «Агенты» (`AgentsPanel.jsx`) | «Ассистент» есть у каждого всегда (`builtin`, удалить нельзя — без него некому отвечать); остальных заводит человек кнопкой внизу. У агента ДВЕ формы (владелец, 2026-09-13: «пункт «Коллекция моделей» удалить — он не имеет смысла; модели добавляются на форме провайдера, без ручного ввода и кнопки «Добавить модель»; список моделей загружается по «Обновить список», нажатие на строку ставит галочку слева, повторное снимает; выбранные модели и есть коллекция»): ПРОВАЙДЕР — общие для всех агентов человека провайдеры (вид API, адрес, ключ) и список моделей от провайдера строками-кнопками (`role="checkbox"`, `aria-label="модель <id>"`), где галочка — про ОТКРЫТОГО агента: `agents[].models` (пары `{providerId, model}`); `providers[].models` — объединение отмеченного всеми агентами, его ведёт клиент в `toggleModel` (ставя — сперва провайдер, потом агент, потому что сервер `cleanPair` принимает в коллекцию только модели, отмеченные у провайдера; снимая — провайдер только когда модель не держит другой агент). Ручного ввода имени модели нет; ранее введённые остаются в списке, пока отмечены. У Ассистента под списком — выбор модели расшифровки `transcribe` из его коллекции; ПАМЯТЬ — своя у каждого (`memory[].agent`, `?agent=`). `modelFor` для чата и бота берёт первую пару коллекции Ассистента с ключом, иначе прежнюю таблицу «задача → модель». Снятая у провайдера модель уходит из коллекций сама. Агент владельца — участник организации (`agent: true`): ему выдают роли в «Ролях» и ставят воркером актива; договор ему не нужен, напоминаний нет (расписания у него нет). Что агент делает с порученной задачей — не решено (вопрос владельцу). |
| Сериализация модели | Вкладка «Инструменты» → «Выгрузка» | `{entities, traits, kinds, funcs, tasks, goals, factors, reports, materials, procs}` — полное состояние как один документ (инвариант 11): и строение системы, и работа по ней. |
| Предел сохранений | `SCENARIO_LIMIT`/`savedRoom` в `storage.js`, подпись под списком сохранённых в «Инструментах» | Пределов два и они разные: 200 на диске сервера (`MAX_SCENARIOS_PER_USER`, число продублировано на клиенте), 30 в облаке Telegram; память браузера предела в сценариях не имеет. Подпись «сценариев: N из M» считает по хранилищу, которое действует сейчас, и предупреждает, когда осталось мало (десятая часть, не меньше трёх) или ничего — до отказа, а не вместо него. |

## 3. Вкладки приложения = стадии работы с моделью

Главных четыре: **Задачи**, **Проверка**, **Схема**, **Инструменты**. Под
схемой — три подвкладки: **Управление** (чем схема собрана), **Прогноз**
(куда она идёт) и **Timeline** (что по ней уже сделано). Все три про одну и
ту же модель, и ползунок месяца у них общий со схемой; держать их наверху
значило разложить одно и то же по нескольким местам. Следствие для прав:
`sim` и `timeline` открываются только вместе со `scheme` — как и было для
`sim` с самого его переезда.

Какие из них показать, решает роль — см. `orgStore.js`; порядок один на всех.

1. **Задачи** — функции модели, под каждой её выполнения, и канбан по
   статусам. Исполнителю здесь видны только его задачи. Сдача записывает
   факт: часы и сколько каждого ресурса взяли и выдали.
2. **Проверка** — то, что проверяет именно этот человек: принять или
   вернуть в работу с отзывом. Принятая сдача идёт в расчёт.
3. **Timeline** — все задачи во времени: бэклог, в работе, на проверке,
   готовые; прошлое и будущее, с пунктиром «сегодня». Задача без начала и
   без сдач на ось не ставится — придумывать ей дату нельзя, — но и не
   пропадает.
4. **Схема** — строение системы и её будущее в одном месте. Сверху холст с
   активами и передачами и ползунок месяца; под ним три раздела в том
   порядке, в каком идёт работа: **«Управление»** — первыми, под
   спойлером, технологические процессы (шаги «что за чем следует», из
   которых собираются функции), затем карточка выбранного актива (три
   вкладки одного вида: воркеры, функции, ресурсы) и классификации
   ресурсов под спойлером; нажатие на актив на схеме всегда ведёт сюда;
   **«Деятельность»** — что по схеме делали (задачи во времени);
   **«Прогноз»** — цели и план под каждую («что нужно сделать»), ленты по
   каждому ресурсу и нагрузка исполнителей. Ползунок общий: он задаёт и
   числа на блоках, и то, до какого месяца дорисован хвост графиков.
5. **Инструменты** — внутренние вкладки: роли (владельцу: участники,
   роли, анкеты — три формы), агенты (Ассистент и другие: провайдер,
   коллекция моделей, память), напоминания, звонки, выгрузка
   (экспорт/импорт модели и сохранение сценариев на диск).

Любая новая фича должна встраиваться в один из этих слоёв (или явно
обосновывать новый слой) — см. `ROADMAP.md`.

## 4. Что добавлено вокруг модели (Telegram + диск) и почему это не меняет парадигму

Расчёт целиком живёт на клиенте: `web/src/lib/funcs.js` (запись и проверки)
и `web/src/lib/plan.js` (прогон по месяцам). Вокруг построена только
инфраструктура доставки:

- **`web/src/App.jsx` + `web/src/telegram.js`** — инициализация Telegram Web
  App SDK (тема, `ready()`, `expand()`). Транспортный слой, к парадигме не
  относится вообще.
- **Дисковое сохранение сценариев** (`server/src/lib/scenarioStore.js` +
  маршруты `/api/scenarios`) — то же понятие «сериализация модели», только
  вместо копипаста JSON — именованное хранилище на сервере.
- **Бэкенд (`server/`)** — обычный Express-сервис: отдать собранный
  фронтенд, хранить JSON, фильтровать модель по роли, слать напоминания,
  держать звонки. Модельной логики в нём нет и не должно быть.

## 5. Инварианты, которые нельзя нарушать будущими изменениями

1. **Функция может только взять и дать.** Третьего действия у неё нет, и
   третьего списка рядом с `takes`/`gives` тоже. Появится потребность в
   «ещё одном» — сначала проверить, не выражается ли она уже имеющимся:
   передача выразилась, и даже не полем, а тем, чей ресурс на выходе.
1a. **Получатель — это сам ресурс.** Куда уходит выданное, говорит
   `traits[].e`, а не поле у выхода. Всё, что уже сказано выбором ресурса,
   спрашивать второй раз нельзя: два места про одно и то же разъезжаются,
   и тогда непонятно, какому верить. По той же причине вход и выход
   выбираются из одного списка ресурсов всей схемы.
2. **Сколько берётся и выдаётся — всегда две границы, но не всегда
   вилка.** В записи `lo..hi` живёт и точное количество (границы равны), и
   гипотеза (границы разные) — второго способа записать то же самое нет,
   иначе появились бы два вида порта и два прогноза по ним. Вилка обязана
   выглядеть вилкой: где она есть, показываются обе границы, и схлопывать
   её в одно число нельзя — это выдало бы за знание то, чего никто не
   измерял. Но и обратное неверно: заставлять писать «от 2 до 2» там, где
   человек знает точное число, значит требовать неопределённости, которой
   у него нет. Поэтому границы разводит галочка «диапазон», а не форма.
3. **План и факт не смешиваются и показываются разными цветами.** План —
   жёлтый, факт — зелёный. Факт берётся из **принятых** выполнений и
   считается средним арифметическим; выдавать план за факт нельзя, и
   наоборот. Пока выполнений нет, линии факта нет вовсе.
4. **Время у функции одно.** Выполнение либо случилось целиком, либо нет:
   раздавать разным выходам одной функции разные сроки не за что.
5. **Воркеры принадлежат активу.** Не функции и не задаче. На функцию
   можно поставить только воркера её актива; человек, переставший быть
   воркером, уходит и с функций (`pruneWorkers`) — иначе задача висела бы
   на том, кого в активе уже нет.
6. **Число исполнителей на скорость не влияет.** Исполнители — те, кто
   функцию выполняет и отвечает за неё, а не множитель производительности.
   Домножать на количество людей значило бы додумать, что двое делают
   вдвое быстрее. Владелец это подтвердил прямо. Понадобится иначе — это
   отдельное решение с записью в `CHANGELOG.md`.

6a. **Расписание не ускоряет работу.** Цикл — большее из двух: сколько
   работа делается и как часто повторяется. Расписание чаще самой работы
   ничего не даёт, и притворяться, что даёт, нельзя.
7. **Ресурс сам себя не меняет.** Его берут и выдают функции. Никаких
   стрелок «ресурс → ресурс», никаких формул на ресурсе: любой новый
   механизм влияния одной величины на другую выражается функцией, иначе
   схема перестанет быть честным отражением модели.
8. **Нехватка делится пропорционально запросу.** Не поровну и не по
   порядку функций в файле: порядок в файле не значит приоритета. И ресурс
   не уходит в минус — взять больше, чем есть, нельзя.
9. **Никакой логики, завязанной на конкретную классификацию.** Код не
   ветвится по их id (`k === "growth"` и подобное): всё поведение
   классификации выражается её полями (`sign`, `name`, `color`, `dir`).
   Ресурс может ссылаться на исчезнувшую классификацию, поэтому обращение
   всегда идёт через `kindOf()`, который возвращает заглушку вместо
   падения.
10. **Проверки строения объясняются словами владельца.** `WHY_ASSET`,
   `WHY_FUNC`, `WHY_TRAIT` — определения, по которым строится модель.
   Красная подпись без объяснения — это «сделай как-нибудь иначе», а не
   подсказка. И проверка не требует того, чего у пользователя может не
   быть в первый день (людей, например): красный, который горит везде,
   перестаёт быть сигналом.
11. **Документ модели — это семь массивов, и они перечислены в одном
   месте.** `entities`, `traits`, `kinds`, `funcs`, `tasks`, `goals`,
   `factors` — то, что
   уезжает в сохранённый сценарий, и ровно то, за чем следят история
   правок (`lib/history.js`) и черновик (`lib/draft.js`). Появится шестая
   часть — её надо добавить и в сохранение, и в `doc` внутри
   `SystemModel`, иначе она молча выпадет из отмены и из черновика.
   Читаются части **мягко**: чего нет в старом документе, остаётся
   текущим, а не поводом отвергнуть документ целиком.
   Позже к семи массивам добавились `reports` (карта отчётов), `materials`
   (единицы, положенные руками) и `procs` (технологические процессы). Запись
   `space` (пространство вкладки задач, 7–9 сентября 2026) из документа
   убрана решением владельца: вкладка задач — только доска.
11a. **Цель — намерение, а не число, и хранится отдельно от ресурса.**
   Ни одно вычисленное по ней число не сохраняется: сколько работы, успеет
   ли, во что обойдётся — всё выводится из модели (`planGoal`). Записать
   рядом «нужно 40 часов» значило бы завести вторую правду: модель
   поправили, а число осталось прежним.
12. **Прежние модели не переносятся.** Записи, собранные под прежний
   расчёт (стрелки между элементами, `qty` вместо вилки, сроки и люди на
   каждом выходе), `normalizeFunc` НЕ читает: владелец решил, что такие
   модели работать не должны. Но и падать приложение не должно — чужая
   запись достраивается до нынешней пустыми полями и краснеет подписью.
   Молчаливый перенос был бы хуже отказа: получилась бы модель, которую
   никто не собирал.
13. **Оценка и отзыв о работе неразделимы, и оба обязательны при приёме.**
   Слова к оценке — «отзыв» (владелец, 2026-09-13), и так они названы
   всюду: «отзыв о постановке» у исполнителя при сдаче, «отзыв к оценке»
   у проверяющего, раздел анкеты «рейтинг и отзывы»; «комментарий» —
   только разговор в задаче (`tasks[].comments`).
   Оценка без отзыва не говорит, что исправлять; отзыв без оценки не
   складывается в историю. Требование стоит и в интерфейсе, и на
   сервере: одна проверка в интерфейсе — это не проверка.
14. **Статистика человека считается, а не хранится.** Средняя оценка, доля
   «в срок», объём работы выводятся из тех же задач, что лежат в модели
   (`lib/workers.js`). Отдельное сохранённое число завело бы вторую правду:
   задачу поправили, а число осталось прежним. И там, где данных нет,
   величина `null`, а не ноль: «неизвестно» — не то же самое, что «плохо».
15. **Запас не заменяет темпа.** У цели с темпом остаток самого целевого
   ресурса в расчёт не идёт: «один клиент в неделю» надо выдавать каждую
   неделю, и двадцать уже имеющихся клиентов работу не отменяют. А запасы
   входов считаются всегда — они настоящие и тратятся на самом деле;
   обнулять и их значило бы потребовать производить то, что приходит
   извне, и тогда недостижимой стала бы любая цель.
16. **Ничего не уезжает на сервер, пока не решено, что открывать.**
   Выгрузка модели владельца (`putWorkspace`) ждёт флага `ready`. Иначе
   встроенная демонстрационная модель успевает уехать поверх настоящей
   работы, и починить это нечем: сервер хранит одну модель. Флаг
   выставляется в ЛЮБОМ исходе автозагрузки — иначе владелец с недоступным
   хранилищем перестал бы синхронизироваться вовсе.
17. **Задачу делают, фактор случается.** Задача не повторяется сама: она
   работа, и выполнения берутся из применённых целей — то есть задача идёт
   в постановку тогда, когда условия позволяют (цепочка входов сходится,
   `solve`) И это отвечает поставленной цели. Фактор наоборот: его никто не
   просит, он идёт своим чередом — столько попыток, сколько помещается, из
   них удаются те, на которые хватило вероятности. «Как часто может повторяться» — потолок, а не
   расписание, и выполнения берутся из применённых целей (`goalRuns`).
   Появится соблазн вернуть самоход «чтобы график был не пустой» — это и
   есть та ошибка: график, который растёт сам, показывает будущее, которого
   никто не планировал, и цель в нём ничего не меняет.
18. **С фактора не спрашивают.** У функции-фактора нет ни исполнителей, ни
   задач, и её часы не тратят ничьё время. Назначить на фактор человека
   значило бы поставить кого-то отвечать за погоду. Если такая потребность
   появится — это не фактор, а задача, и переключатель для этого и стоит.
19. **Прогон дискретный, шаг = 1 месяц.** Понадобится более точный шаг —
   это отдельное архитектурное решение, которое надо явно обосновать в
   `CHANGELOG.md`, а не тихо поменять внутри `runSide()`.
20. **Одну единицу — одной функции только раз, и это правило ОДНОЙ
   функции.** Расходует вход — единица исчезает у всех, и спорить не о чем.
   Не расходует — единица остаётся на месте и достаётся другим функциям, но
   эта по ней уже отработала: прочитанную заявку не читают второй раз.
   Появится соблазн снова тратить всё и всегда «чтобы считалось проще» —
   это и есть та ошибка: тогда прочитанное одной функцией пропадает у
   соседей, которые его и не трогали.
22. **Цель применяется один раз.** Второе применение той же цели — это
   вторая цель: копия рядом в списке, со своим временем применения и своей
   работой. Дописывать задачи в уже применённую значило бы сложить в одну
   запись два разных результата, и разобрать их обратно было бы нечем.
   Удаление цели убирает её незаконченную работу и пересчитывает прогноз
   целиком; сданное остаётся — оно уже случилось.
23. **Прикидка отчёта не трогает модель.** Правка вилок в шаге живёт в
   разделе (`reports[].tweaks`) и никогда не записывается в `funcs`. Отчёт
   спрашивает «а если», а не переучивает схему: перенеси прикидку в модель —
   и соседний отчёт, который её не просил, посчитается по чужому допущению.
   По той же причине пустое поле прикидки означает «значение модели», а не
   ноль.
22a. **Сделанное не стирают.** Работу отменяют — задача остаётся с пометкой,
   вместе со сдачами и оценками. Удаление задачи уносило бы то, что и правда
   было; отменённая же нигде не считается работой, и это разные вещи.
23c. **Функцию правят через одну дверь.** `editFunc` — единственное место,
   где функция меняется: форма, удаление ресурса, удаление актива, снятие
   воркера, удаление фактора. Она же снимает «принято» — и только при
   НАСТОЯЩЕМ изменении. Сброс, разложенный по местам правки, забывается при
   первой же новой кнопке, а безусловный сброс снимал пометку от клика мимо
   поля и писал в историю шаг, которого никто не делал.
23b. **Проверка строения и слово человека — разные вещи.** «Собрана» считает
   программа (`funcGaps`), «принята» ставит человек (`accepted`). Выводить второе из
   первого нельзя: тогда пометка означала бы «программа не нашла ошибок», а не «я это
   сделал и проверил». И обратно: правка снимает принятие, иначе метка врёт.
24a. **Звено цепочки не исчезает молча.** Шаг, который не выполнится,
   остаётся в отчёте с нулём выполнений и названной причиной. Отчёт,
   показывающий обрубок цепочки вместо неё самой, не даёт человеку понять,
   у него модель такая или программа врёт, — а это первое, что он
   спрашивает.
25. **Раздел отчёта — это выполняемая функция.** У него тот же набор
   данных, что и у отчёта целиком: оценка, прогноз ресурсов, задачи. Ссылка
   есть у раздела, а не у части страницы: ссылаются на «передачу заказа
   разработчикам», а не на «первый блок сверху». Руками разделы не заводят —
   кнопки для этого нет: придумывать второй способ завести раздел значило бы
   завести два разных ответа на «что такое раздел».
25a. **Отдельного списка «созданные ресурсы» нет.** Вопрос «что уже вышло»
   задают не вообще, а про конкретный ресурс: «а договоров-то сколько
   реально получилось». Поэтому вещи открываются нажатием на строку своего
   ресурса прямо в прогнозе — и там же скачиваются. Второй список отвечал бы
   на тот же вопрос второй раз и в другом порядке.
25b. **Каждая величина названа полностью и стоит своей строкой.** «выполнений
   1 · начнётся через мгновенно · займёт 1 дн · работы 24 ч» прочесть нельзя:
   непонятно, где кончается одно число и начинается другое, и что за величина
   названа. Слева вопрос («Кол-во выполнений ожидается»), справа ответ. И ноль
   часов до старта — это не «мгновенно», а «сразу после постановки».
25c. **Таймлайн стоит на календарной линейке.** Под шкалой пять делений с
   датами, и у каждой полосы её даты словами. Без них «начнётся через 1 ч» и
   «займёт 1 мес» человек складывает в уме, а на какое число попадает конец —
   сказать не может вовсе.
26. **У каждого ресурса на графике своя шкала.** Общая шкала для дохода в
   сотнях тысяч и договоров в штуках превращает договоры в невидимую
   чёрточку, а складывать их незачем — сравнивают план с фактом. И числа
   стоят текстом рядом с названием, а не подписями внутри картинки: подписи
   вылезают за край формы ровно тогда, когда значения большие.
23a. **Одновременность — свойство функции, а не числа людей.**
   `par` говорит, сколько таких дел ведёт разом ОДИН воркер, и на число
   исполнителей не умножается (инвариант 6 в силе). `parAll` — второй
   предел, на весь актив; в календарь ложится меньший из двух, а пустой
   `parAll` не ограничивает ничего. Одно исключение — и оно называется
   вслух: `parCrew` привязывает ПОТОЛОК АКТИВА к числу воркеров, и тогда
   человек в активе этот потолок поднимает. Это прямое решение владельца,
   и оно не отменяет инвариант 6: множителем число людей не становится
   нигде — «на воркера» на него по-прежнему не умножается, а потолок сам
   по себе работу не ускоряет, он только перестаёт её сдерживать. Все три следствия
   считаются из одного правила «одно выполнение занимает 1/par времени
   воркера»: потолок за месяц, волны в расписании и часы на человеке.
   Считать их по разным правилам — значит получить отчёт, который сам с
   собой спорит.
24. **Спросили про вещи — отвечаем про них; не спросили — про всю цепочку.**
   Выбраны единицы: задача над другим договором к вопросу «что будет с
   этим» отношения не имеет, даже если её делала та же функция. Не выбрано
   ничего — вопрос другой, «что вообще делается по этой цепочке», и
   отвечать на него пустотой нельзя: задачи, созданные вещи и часы есть, и
   они показываются. Прогноз при этом всегда подписан прогнозом, а факт —
   фактом: число без подписи не говорит, обещание это или измерение.
24b. **Результат работы прикладывается при сдаче, и без него работа не
   сделана.** Функция выдаёт не число, а вещь: техническое задание, макет,
   договор. Исполнителю при сдаче предлагается приложить каждый выданный
   ресурс отдельно (`submissions[].files`), и выход, у которого нижняя
   граница вилки больше нуля, обязателен — «Сдать» не нажимается, и сказано,
   чего не хватает. Ноль внизу вилки — прямое разрешение не выдать ничего,
   и требовать файл там не за что. Один общий файл отчёта на все выданные
   вещи означал бы, что скачать сам макет неоткуда, — он остаётся запасным
   для сдач, сделанных до этого.
21. **Номер единице даёт сдача, а не рука.** Единица ресурса рождается
   выполненной работой: у неё есть автор, дата, текст и файл. Поле «завести
   единицу вручную» означало бы вещь, которой никто не делал, — и первое же
   расхождение с задачами было бы неразрешимым: непонятно, какая из двух
   записей правда.
27. **Текст процесса — единственный источник его функций.** Функция с
   пометкой `proc` пересобирается из текста при каждой его правке и смене
   статуса; править её рецепт и исполнителей в карточке можно, но текст
   сильнее (положение на схеме, постановщики и проверяющие — остаются).
   Шаги — производные и руками не правятся; замена удалённого — правка
   слова в тексте, не id в шаге. Иначе появилось бы два места для одного и
   того же, и они разошлись бы на первой же правке.
28. **Ответ привязан к вопросу идентификатором, а не текстом и не номером.**
   Правка формулировки, перестановка и добавление вопросов анкеты не должны
   ни терять, ни переназначать ответы; удалённый вопрос уносит свой
   идентификатор, и новый вопрос ответа не наследует. Ответы пишутся поверх
   прежних, а не вместо: сохранение одной анкеты не стирает ответы на
   вопросы другой.
29. **Общие часы графика остаются общими.** `perDay` — только исключения: у
   дня без записи часы общие, и записывать общие часы в каждый день «для
   верности» нельзя — тогда правка общих перестала бы что-либо менять, а
   текст графика распался бы на семь групп.
30. **У единицы ресурса одна ссылка на скачивание на всё приложение.**
   Файл — `reportSrc`, текст и код — `textHref` (`storage.js`); новое место,
   где единицу дают скачать, использует `UnitLinks.jsx`, а не свою ссылку.
31. **Подстановка исполнителя и проверяющего по порядку списка — только в
   пустое поле задачи в статусе `wait` и только один раз на открытие
   задачи.** Назначенного рукой и поставленную задачу форма не
   переигрывает. Сервер и бот никого не подбирают.
32. **Роль действует, пока действует подписанный договор.** Вкладки
   человеку дают только действующие роли: с документом — по подписанному
   соглашению, чей срок начался и не кончился; отключённая на «Участниках»
   роль не действует. Договор выдаётся только заполненным: пустой
   плейсхолдер — отказ при подписи, а не «потом допишем». Подпись — не
   картинка, а траектория с временем и нажимом, подписантом, моментом и
   хешем документа: по ней можно сказать, кто и что подписал.
