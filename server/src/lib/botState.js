/* ════════════════════════════════════════════════════════════════
   СОСТОЯНИЕ ИНЛАЙН-РЕЖИМА БОТА — для /api/health

   «Я вообще не вижу инлайна» (владелец, 2026-09-25) — причин три, и
   снаружи они неотличимы: инлайн не включён в @BotFather (Telegram
   тогда запросов не шлёт вовсе), запросы приходят, но ответ падает,
   или Telegram не принимает ответ. Здесь — ровно то, что их различает:
   что сказал getMe, сколько запросов пришло и чем кончился последний
   ответ. Ничего про людей: ни id, ни текста запросов.
   ════════════════════════════════════════════════════════════════ */
export const botState = {
  inlineSupported: null,   // getMe.supports_inline_queries; null — ещё не спросили
  inlineQueries: 0,
  inlineLastAt: null,
  inlineAnswered: 0,
  inlineError: null,       // последний отказ Telegram или ошибка обработки
};
export const inlineView = () => ({ ...botState });
